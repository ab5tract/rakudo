# Milestone 4 final fix wave — brief

The final whole-branch review is at `final-review.md` beside this file: READ IT FULLY FIRST. It contains the findings below with file:line evidence, the root-cause analysis and the fix shapes; this brief only lists what the wave must do, the controller's rulings, the order, and the gates. The gate report `task-11-report.md` has the 11 red files and the reproducers.

Trees: rakudo worktree `/home/longwalker/code/raku/x.core/rakudo/.claude/worktrees/jesp-direct-lazy-records` (HEAD e9a5b9205a) and the NESTED `nqp/` tree (HEAD d24431b68). Start state: both clean.

## Must do (in this order)

### 1. C1 — restore `is_inlinable` (compiler-side, both trees)
Per final-review C1, exactly:
- `nqp/src/vm/jvm/QAST/Compiler.nqp` next to the registries (~:199): a `%core_noninlinable` table seeded with the twelve op names that carried `:!inlinable` in the deleted `add_core_op` calls: `call`, `callstatic`, `dispatch`, `syscall`, `register`, `delegate`, `track`, `guard`, `handle`, `handlepayload`, `usecapture`, `savecapture` (verify each against `git show 55bdee5b7:src/vm/jvm/QAST/Compiler.nqp` from the nqp dir, grep `:!inlinable`; add any the reviewer missed, drop any it invented).
- `is_inlinable($hll, $op)` (~:216-223): HLL override if present (as now); else `%core_inlinability{$op}` if the key exists; else 0 if `%core_noninlinable{$op}`; else `QAST::TruffleEncoder.supports_op($op) ?? 1 !! 0`.
- `src/vm/jvm/Raku/Ops.nqp` (rakudo): after the `register_op_desugar` sub, five lines `$ops.set_hll_op_inlinability('Raku', '<op>', 0)` for `p6bindsig`, `p6trybindsig`, `p6decontrv`, `p6decontrv_6c`, `p6return` (the deleted `add_hll_op(..., :!inlinable)` rows). While there: delete the dead `$EX_CAT_NEXT/REDO/LAST`, `$RT_VOID`, `$RT_UINT` constants if unreferenced.
- Probe BEFORE the make, after the nqp clean build: `RAKUDO_RAKUAST=1 nqp/nqp-j-gradle -e 'say(nqp::getcomp("QAST").operations.is_inlinable("Raku","add_i"))'` and the same for `if`, `while`, `list`, `callmethod` (expect 1) and `call`, `handle`, `syscall`, `ctx` (expect 0); the Rakudo-side `p6return`/`p6bindsig` = 0 can only be probed after the make (`./rakudo-j -e 'say nqp::getcomp("QAST").operations.is_inlinable("Raku","p6return")'` — nqp:: ops are available in Raku via `use nqp;`).

### 2. I3 — `$extra_ops`' three roles named once
`nqp/src/vm/jvm/QAST/TruffleEncoder.nqp:56-62` and `:520-535`: one comment naming the three consumers (survey coverage, `supports_op`, inlinability via `is_inlinable`'s fallback). No behaviour change.

### 3. Wave-riding comment/dead-code items (same files, same rebuild)
- `Compiler.nqp`: delete `$RT_VOID` (~:163), `@typeobjs`/`typeobj_from_rttype` (~:177,:182), `@typechars`/`typechar` (~:178,:183) if unreferenced; `unit(... *%adverbs)` drop the unused slurpy if nothing passes adverbs.
- Stale JAST names in comments: `TruffleEncoder.nqp:1934`, `:2154` (`add_core_op`), `:428-433` (the "stage0 reads the mere PRESENCE of NQP_CODE_RUN" paragraph: now false; delete it, keep the `=0` die's own comment), `:1184-1195` (name "any deferred code-ref slot the prologue records", and fix the inverted sentence); `RxDescriptor.nqp:211` (`engine_jast`) and `:759` (`as_jast(QAST::Var)`); `nqp/src/vm/jvm/runtime/org/raku/nqp/runtime/unit/ProgramEntry.kt:17`; `nqp/nqp-truffle/.../NqpCursor.kt:67`; `NqpLanguage.java:16` / `NqpRootNode.java:22` (reword as history).
- `NqpTypeOps.kt:407-409`: the comment covers the object decont only; say so.
- `NqpOps.java:1485`: drop the `NQP_DO_TRACE` gate on the sited `bindattr` (it double-traces through `bindattrSlow`).

### 4. C2 — the string-range hang, investigated in PARALLEL with the C1 make (no build needed for the first experiment)
Per final-review C2. Reproducer: `timeout 60 env RAKUDO_RAKUAST=1 ./rakudo-j -e 'say ("aa".."ac").elems'` (rc 124 today). The reviewer proved a verbatim copy of `SEQUENCE.rakumod` compiled as a script or a precompiled module works, so the fault is in CORE.c's build of that routine. Experiments in this order:
1. Dump/diff the inner gather block's record and program between `blib/Perl6/CORE.c.setting.jar` and a precompiled copy (`/tmp/claude-1000/m4lib/.precomp/...` from the reviewer's probe; `/tmp/claude-1000/m4-myseq.raku` is the copy; if gone, recreate: copy `src/core.c/Rakudo/SEQUENCE.rakumod` under another name into a module and precompile it with `./rakudo-j -Ilib -I<dir> -e 'use MySeq; say ("aa".."ac").elems'` style, see `final-review.md` C2). Use `org.raku.nqp.runtime.unit.UnitZip.read(bytes)` from a small Kotlin/Raku or jshell driver (a jshell session over `nqp/build/jvm/share/lib/../nqp-runtime.jar` and the third-party jars: `jshell --class-path ...`), print `meta.blocks[qbid]` fields and `programs[programIndex]` for the block whose name/line matches `SEQUENCE.rakumod` around line 286-350 (the inner gather / `until $stop` loop). Identical program text + different records -> the driver (`Compiler.nqp` comp-mode paths: qbid pre-seeding, orphan blocks under the deserialize wrapper `:1455-1466`, static-lexical rows `:1519-1531`, blanked cuids `:1766`); different text -> the encoder in the setting's environment (e.g. `$*COMP_MODE` differences in `encode_block`); identical both -> loading (`applyStaticLexValues`, outer resolution, `claimNested`).
2. After the C1 make: re-run the reproducer (a CORE.c-only consequence of C1 is possible).
3. Only if both fail to explain it: runtime-jar bisection over nqp `d8116d7c9` (pre-Task 9) then `14df06863` (pre-Task 8), ~30 s each, eval servers restarted.
Fix at the cause. If the fix is compiler-side and found only AFTER the C1 make started, a SECOND make is authorized (correctness beats compile time). Time box for C2: the duration of the C1 make plus one hour; if unresolved, STOP and report it as open with everything established (the controller rules whether the milestone ships with a ledgered hang).

### 5. I1 — exit handlers of continuation-captured frames (runtime-only)
Per final-review I1: the engine's save path calls `cf.leave()` on every frame it packs into a continuation (`NqpCodeEngine.java:205-207, 215, 227, 241`), which now sets `left` and runs the exit handler once with a stale result, then never at the real exit. Fix: the save road must not count as the exit for handler purposes — give `CallFrame` a `leaveSuspended()` (count given back / `priorInvocation` bookkeeping as `leave()` does, NO exit handler, `left` not set for handler purposes) used by those save sites, so the real exit (normal `leave()` or `leaveTorn()`) runs the handler once with the right result. Read `leave()`/`leaveTorn()`/`runExitHandler` (`CallFrame.kt:440-490`) and the four call sites first; keep `liveInvocations` correct (decrement exactly once per frame across suspend/resume: check what `resumeEngine` does on re-entry). Test: `RAKUDO_RAKUAST=1 ./rakudo-j -e 'my @a = gather { LEAVE say "done"; take 1; take 2 }; say @a'` must print `done` exactly once, AFTER `[1 2]`'s production (i.e. once, at the gather block's real exit), plus `keep-undo.t` 16/16 and `t/spec/S04-phasers/enter-leave.t` unchanged.

### 6. I2 — `UnitLoader.load` marks a unit loaded before the load succeeds
`nqp/src/vm/jvm/runtime/org/raku/nqp/runtime/unit/UnitLoader.kt:67`: add to `loadedUnits` after `loadAndRun` returns (or remove on the exception path).

### 7. I5 — the param-default regression test
`t/02-rakudo/begin-time-attributive-param-method.t`: `plan 5` -> `plan 6`; add a test that `BEGIN { sub g($x = { 42 }) { $x() }; g() }` answers 42 (write it in the file's style).

### 8. Registry test (final-review recommendation 2)
A new nqp test `nqp/t/jvm/10-op-registry.t` (or the next free number; check `nqp/t/jvm/` and how the JVM-only tests are listed — `nqp/build.gradle.kts` `testNqp` and `nqp/tools/templates/jvm/Makefile.in`'s test list) asserting `nqp::getcomp("QAST").operations.is_inlinable('nqp', $op)` = 1 for `add_i`, `if`, `list`, `callmethod` and 0 for `call`, `handle`, `ctx`, `syscall`, and `nqp::getcomp('nqp').supports-op($op)` = 1 for a handful of `$extra_ops` names (`index`, `sprintf`, `settypefinalize`) and 0 for `nosuchop`. Run it with `nqp/nqp-j-gradle`.

### 9. I4 — timings and docs (rakudo tree, after the make)
Re-record: make total and CORE.c window from the new make's markers; note in `docs/jvm-truffle-only-plan.md` (row 6 / the milestone-4 text), `docs/superpowers/specs/2026-09-10-jvm-unit-artifact-milestone-4-design.md` (Done section) and `docs/superpowers/specs/2026-09-09-jvm-unit-artifact-design.md` (Milestones item 4) that the earlier numbers were measured with the inliner idle (C1) and give the new ones; update the regression statements to their outcome (fixed / open); the ledger twin `docs/superpowers/plans/2026-09-10-jvm-unit-artifact-milestone-4.ledger.md` gets the fix-wave lines; copy `final-review.md` and this brief into `docs/superpowers/plans/2026-09-10-jvm-unit-artifact-milestone-4.reports/`.

## Builds and gates (forward only)
- nqp: `./nqp/gradlew -p nqp :nqp-runtime:test :nqp-runtime:jar :nqp-truffle:jar syncRuntimeJars` for the runtime items (5, 6, 3's runtime comments) — do these first, they are seconds; then ONE `RAKUDO_RAKUAST=1 NQP_CODE_STRICT=1 raku tools/build/watched-run.raku --log=/home/longwalker/.claude/jobs/25fa1a35/tmp/fw-build.log --show-file=/home/longwalker/.claude/jobs/25fa1a35/tmp/fw-build.markers --show='> Task :stage' --show='code-bail' --show='BUILD' --show='rror' -- ./nqp/gradlew -p nqp clean buildJvm` (~4 min) carrying items 1-3; then the registry probe; then t/nqp + the new registry test (`RAKUDO_RAKUAST=1 raku tools/build/watched-run.raku -t=nqp/t/nqp -t=nqp/t/jvm --jobs=3 --log-dir=/home/longwalker/.claude/jobs/25fa1a35/tmp/fw-nqp-logs --show-file=/home/longwalker/.claude/jobs/25fa1a35/tmp/fw-nqp.markers -- nqp/nqp-j-gradle`; expected 118/118 (019/063 from the nqp dir) + the jvm tests green (09-autosplit is gone)).
- rakudo: `RAKUDO_RAKUAST=1 raku tools/build/watched-run.raku --log=/home/longwalker/.claude/jobs/25fa1a35/tmp/fw-make.log --show-file=/home/longwalker/.claude/jobs/25fa1a35/tmp/fw-make.markers --show='Compiling' --show='Generating' --show='rror' --stall=1500 -- make` (~20 min; the compiler changed, so everything after rakudo.jar recompiles; NO Configure needed: nqp's stage2 SC handles change on a clean build -> if the make fails with "Missing or wrong version of dependency", run `perl Configure.pl --backends=jvm --gen-nqp` first and then make from the top — actually DO run Configure first to be safe, it costs 3 s and cleans the jvm products: `RAKUDO_RAKUAST=1 raku tools/build/watched-run.raku --log=/home/longwalker/.claude/jobs/25fa1a35/tmp/fw-configure.log -- perl Configure.pl --backends=jvm --gen-nqp`).
- After the make, the sensitive slice (final-review recommendation 3), cold, `./rakudo-j -Ilib`: `t/08-performance/22-rakuast-ct-dispatch.t`, `29-rakuast-attr-self-types.t`, `32-rakuast-native-param-bind.t`, `t/02-rakudo/native-return-coercion.t`, `t/02-rakudo/sort-element-kinds.t` (under `--max=600 --stall=300`), `t/02-rakudo/yada-trait-timing.t`, `t/02-rakudo/begin-time-attributive-param-method.t`, the C2 reproducer, the I1 gather probe, `t/spec/S04-phasers/keep-undo.t` and `enter-leave.t` (from `/home/longwalker/code/raku/x.core/rakudo/t/spec/`), then `t/01-sanity` (2 jobs), `t/03-jvm` + `t/10-qast`, precomp (the 14-file list in task-11-brief.md; `RAKUDOLIB=lib` for `rakuast-suspend-precomp-deps.t`). Also the remaining red files from task-11-report.md that were not root-caused: run each once cold and record pass/fail (no fixing unless trivial and obviously ours).
- Restart/kill eval servers after any runtime jar rebuild; never set `NQP_CODE_RUN`/`NQP_CODE_PRECOMP`; `RAKUDO_RAKUAST=1` everywhere.

## Commits
Separate commits per concern, in the tree the change lives in: (a) nqp "compiler: is_inlinable answers from the classlib registry, the encoder's rows and an explicit non-inlinable table -- the deleted add_core_op closures were the table's source" (items 1-3 nqp side); (b) rakudo "JVM ops: the five non-inlinable Raku ops are recorded again" (+ dead constants); (c) nqp "runtime: a continuation-captured frame keeps its exit handler for its real exit; UnitLoader marks a unit loaded only after it loaded" (items 5, 6, the NqpOps gate); (d) nqp "tests: the op registry (inlinability, supports-op)"; (e) rakudo "tests: a BEGIN-time parameter default holding a code object" (I5); (f) C2's fix in its tree with a message naming the cause; (g) rakudo docs (item 9). Each with the two attribution lines:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_012XTGZcNxJyimav3icXCq9U

## Report
`fix-wave-report.md` beside this brief: per item what changed and the covering test with command + output; C2's experiments and outcome; the new timings; commits. Then the short status contract.

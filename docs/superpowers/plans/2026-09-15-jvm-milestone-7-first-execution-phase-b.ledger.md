# SDD ledger — plan: docs/superpowers/plans/2026-09-15-jvm-milestone-7-first-execution-phase-b.md

Spec: docs/superpowers/specs/2026-09-13-jvm-milestone-7-first-execution-design.md
(rakudo 83375a77ae), Phase B, over the lazy-loading spec's tasks 1.1-1.6
(docs/superpowers/specs/2026-09-13-jvm-lazy-unit-loading-design.md, Revision 3).
Plan committed rakudo 36ce45c326 (2026-09-15), pushed to ab5tract.

BASE before Task 1: rakudo 36ce45c326, nqp fdab66706 (Phase A's close; the
a6 rig row: cold rakudo-e 2.504 s, cold nqp-e 1.192 s, misses 5661, hits
100697).

Claude-Session trailer for this phase's commits: not known to the writing
session; Phase A's URL (session_01GK4DKrQFQgNaiYirUzjcgJ) is NOT reused.
Commits carry the Co-Authored-By trailer only until the controller learns
its own URL (user rule: never fabricate).

Model policy (user rule 2026-09-11): subagents on Opus; Fable only after
erroneous output; escalations logged here.

The plan's twelve rulings (open to the user's veto until Task 3 writes the
format):
1. v1 is fully deflated; v2 entries are stored (CORE.c jar ~5.9 -> ~13 MB), sizes recorded not reduced.
2. unit.serialized is LZ4'd today; v2 writes it raw and hands the mapped slice to the reader.
3. The call-site table is dead; v2 drops it; Phase C carries the descriptor inline.
4. Every shell is built eagerly (the reader needs 97 % of them); bodies are what get lazy.
5. Static lexical values live per block and are applied on body fill, queued until the SC is ready.
6. Roots have no unit identity today; the compile key "<unit>#<index>" names the Source for store-backed units; in-memory units keep the text key and get no identity.
7. Loop bodies are emitted twice; the ordinal is keyed by the DISPATCH node's wire offset; the encoder's per-block count sizes the slot table; NQP_SITE_CHECK compares.
8. Helper, rv-decont and indy sites have no identity; the schema tolerates it.
9. In-memory units take the same store road through a heap image; v1 is transcoded into that image during the window.
10. The mh identity test at CallFrame.kt:40,320 and Syscalls.kt:396,409 becomes staticInfo identity before mh goes lazy.
11. kotlinx AbstractEncoder/AbstractDecoder under @OptIn(ExperimentalSerializationApi) (deviation from "stable API only").
12. Gates: window build = nqp suite + make + t/01-sanity + ONE warm t/02-rakudo before stage0 regeneration; steps 2 and 3 = nqp suite + make + t/01-sanity.

## Pre-flight scan (controller, before Task 1)

Pairs sharing a file or an interface:

| tasks | produced / consumed | found |
|---|---|---|
| 2 / 3 | `UnitCodec.encode(serializer, value): ByteArray`, `decode(serializer, ByteBuffer)` | consistent |
| 3 / 5 | `UnitStore` (open/isUnit/programIndex/outerQbid/blockRecord/program/dispatchSlot/serialized/nested/entry), `UnitImage`, `BlockEntry`, `UnitImageWriter.bytes/write` | consistent; `entry()` maps `unit.X` to `nested/<id>.X` for a nested store |
| 3 / 6 | `dispatchCounts` per program from `BlockRecord`'s `dispatches` | consistent (RecordReader maps block count to its program index) |
| 4 / 5 | `CodeRef(compUnit, mh, name, uniqueId, argsExpectation, source)`, `StaticBodySource.fill`, `finishBody()` internal, setters never fill | consistent; `staticCode` is set by the CodeRef shell ctor (`this`) |
| 4 / 6 | none (Task 6 reads only shell fields `programIndex`, `compUnit`, `methodName`) | consistent |
| 5 / 6 | `CodeEngines.materialize` tests `cu is ProgramUnit`; `UnitWriter.store`; `RecordReader.intOr("$!dispatches", 0)` | consistent |
| 5 / 7 | Task 7 Step 5 adds `unit-check` to `ProgramUnit.buildTable` | consistent |
| 5 / 9 | Task 9 deletes `UnitZip`/`UnitRecord`/`UnitFormat` and the transcoder Task 5 added | consistent |
| 2 / 9 | Task 2 adds kotlinx to `NqpDeps`; Task 9 removes lz4 if unused | consistent |
| 1 / 7 | Task 1's gradle/rakumod edits are first compiled by Task 7 | consistent |
| 6 / 7 | `site-check` (engine) and `unit-check` (runtime) lines under `NQP_SITE_CHECK` | consistent |

Each task against itself:

| task | check | found |
|---|---|---|
| 1 | stage classpath set at configuration time with `thirdPartySorted()` | **defect**: resolving the configuration at configuration time; Ruling below |
| 2 | `@Serializable data class N` declared inside a test function | **defect**: the kotlinx plugin does not serialize local classes; Ruling below |
| 2 | layout test bytes vs codec (`2,0,0,0,'a','b',4,3,2,1`; null mark `[0]`, `[1,0,0,0,0]`) | consistent |
| 3 | test expectations vs `UnitStore` messages ("unit.programs index 3", "unit.records index 4", "version 9", name "<cut>") | consistent |
| 3 | `program(0).length == 70006` = "PROG0 " + 70000 | consistent |
| 4 | `cloneFillsFirst` expects distinct `oLexStatic` after clone | consistent with `clone()` |
| 4 | `finishBody` internal, called from the test source set | consistent (same module) |
| 5 | `staticLexValuesWaitForTheSc` needs `deserializeQbid = -1` and an SC "sc-x" with an object at 7 | consistent, fixture built by `unitWithSc()` |
| 6 | Java calls `identity.site(ordinal).getKey()` on a Kotlin value class | **defect**: a value class is its underlying type in Java; Ruling below |
| 6 | `siteOrdinals.computeIfAbsent(at, k -> siteOrdinals.size())` | reads the map inside the mapping function only; allowed |
| 7-9 | commands and gates | consistent with the Global Constraints |
| 10 | rig `--tag=b`, default `--warm=proxy` | consistent with the rig's MAIN |

Rulings from the scan:
- Ruling: Task 1 sets the stage tasks' `classpath` inside `doFirst` (where the old `-Xbootclasspath/a` string was built), not at configuration time — `thirdPartySorted()` resolves the `nqpThirdParty` configuration, which must stay lazy — costs if wrong: a configuration-cache warning, nothing functional.
- Ruling: Task 2's `nullMarkIsOneByte` declares `N` as a nested `@Serializable data class` of the test class, not a local class — the kotlinx compiler plugin rejects local serializable classes — costs if wrong: nothing.
- Ruling: Task 6 exposes `ProgramIdentity.siteKey(ordinal: Int): String` and the Java builder calls that; `SiteIdentity` stays as the Kotlin-side value class — costs if wrong: nothing.
- Ruling: the SDD workspace's `progress.md` is a symlink to this file; this file is the one ledger (Phase A's convention) — costs if wrong: nothing.

## Task log

Task 1: complete (commits nqp fe007f3f8, rakudo = the commit carrying this ledger; built by Task 7)
Task 1 note: this phase's commits carry only the `Co-Authored-By` trailer — the session URL for the `Claude-Session` line was not available to Task 1's session, so no such trailer was written.
Task 1: review (Opus): spec compliant, quality approved; stamps 20:30/20:31 +02:00 and the single Co-Authored-By trailer verified by the controller. rakudo hash of Task 1 = 107eca63a3.
Task 1: minor (deferred): three boot-class-path comments elsewhere in nqp/build.gradle.kts (:108, :273, :287) still say "boot classpath" where the mechanism now lives on the class path; the configuration-time `classpath = files(compilerDir, engineJarFile)` at :231 is kept as the task's input snapshot but uncommented. Ruling: both ride on Task 2's dispatch (it edits the same build file for the kotlinx plugin) -- costs if wrong: nothing.
Task 1: minor (deferred): the runtime still looks the engine up reflectively (CodeEngine.kt:172, GrammarEngine.kt:198, Class.forName via the system loader) although nothing is on the boot class path any more; removable in a later phase, not this one.
Task 1: complete (commits nqp fdab66706..fe007f3f8, rakudo 36ce45c326..107eca63a3, review clean)
BASE before Task 2: rakudo 107eca63a3, nqp fe007f3f8.
Task 2: complete (nqp 5ac700d1e; 4 codec tests; runner starts)
Task 2: implementer DONE (nqp 4f546bf6b); concern "the Makefile THIRDPARTY_JARS path will not see the new jar" checked by the controller: the Makefile's NQP_JARS/BLD_NQP_JARS are generated by Configure.pl from nqp/build/jvm/share/.../jvmconfig.properties, whose runtime.jars already lists kotlinx-serialization-core-jvm-1.11.0.jar; Task 7 runs Configure.pl before make, so no action.
Task 2: minor (deferred): NqpDeps.runnerJars' KDoc still says "bootclasspath".
Task 2: review (Opus): spec compliant; NEEDS FIXES -- Important: UnitCodecTest.kt committed with a raw NUL byte (the VCS sees it as binary; the diff could not show the test), and the report claimed the file was escape-clean. Fix round 1 dispatched (amend, escapes, re-run the 4 tests).
Task 2: minor (deferred): present-nullable decode, empty collections and a >64 KB string are not covered by the codec tests (Task 3's records exercise them); UTF_8.decode replaces malformed input with U+FFFD rather than throwing (a corrupt artifact decodes to garbage unless the length prefix is out of range) -- a decision for Task 3's index tables; rakudo-j does not carry the new jar until Configure.pl re-runs (Task 7).
Lesson (controller): the Write tool turns a "\u0000" escape in a code block into a real NUL byte; briefs containing that escape must warn the implementer to write it through sed/python. Recorded for any later brief.
Task 2: fix round 1/5 (2 addressed, 0 open -- NUL byte in UnitCodecTest.kt, false report paragraph; commit 4f546bf6b amended to 5ac700d1e)
Task 2: minor (deferred): the fix report's substitution table (task-2-report.md:242-247) shows decoded characters instead of escape text (untracked file, nothing to fix in the tree).
Task 2: complete (commits nqp fe007f3f8..5ac700d1e, review clean after round 1)
Ruling (for Task 3): UTF_8.decode's replacement of malformed input stays (the codec reads data the writer wrote; the index's (offset, length) tables bound every read, so corruption surfaces as a bounds error or a decode exception, both hard errors naming the unit) -- costs if wrong: a corrupt artifact could decode a program text to garbage instead of failing at open; the engine's wire decoder then fails on it.
BASE before Task 3: rakudo 107eca63a3, nqp 5ac700d1e.
Task 3: complete (nqp d57d5aad4; 7 store tests)
Task 3: review (Opus): spec compliant, quality approved; four named risks (STORED fields before putNextEntry, byte order on every getInt path, EOCD scan bounds, nested prefix mapping) all clean; stamp 20:40 +02:00 and the single trailer verified by the controller.
Task 3: minor (deferred): corrupt-index reads on hand-corrupted artifacts can escape as unnamed IndexOutOfBounds (dispatchSlot never checks firstSlot+slotCount <= dispatchSlotCount; negative offsets pass the `off + len > remaining` guards; ZipDirectory's cen/loc unbounded below) -- unreachable for writer-produced artifacts; a Phase C note when slots become writable.
Task 3: minor (deferred): UnitStore.open(ByteBuffer) reads from index 0 while isUnit honours position(); a `require(bytes.position() == 0)` would close the asymmetry (Task 5's callers all wrap at position 0).
Task 3: minor (deferred): heap-opened stores hand out writable slices (asReadOnlyBuffer on the slices would match the mapped path); `require(b.outerQbid < nb)` has no lower bound; an empty dispatch-slot payload is indistinguishable from an empty slot (Phase C decides); nesting is one level deep, as v1 was; no test for a zero-block unit, a gap at qbid 0, `entry()` or ZipDirectory's non-STORED refusal.
Task 3: complete (commits nqp 5ac700d1e..d57d5aad4, review clean)
BASE before Task 4: rakudo 107eca63a3, nqp d57d5aad4.
Task 4: complete (nqp 94d74785f; lazy tests 5/5; nqp suite 155 green in 189 s)
Task 4: review (Opus): spec compliant, quality approved; JMM publication through the volatile bodyReady write holds; Java call-site conversion complete (grep over both trees: zero remaining field reads); clone path forced ready; stamp 20:45 +02:00 and the single trailer verified by the controller.
Ruling: `_sourceLine` keeps its pre-existing default -1, not the plan's 0 (the plan's value was a transcription slip: -1 is guest-visible through getcodelocation and named in the KDoc the plan says to keep) -- costs if wrong: nothing.
Task 4: minor (deferred -> folded into Task 5's dispatch, same file): finishBody() is not idempotent (a second call rebinds _mh twice) and is `internal` with a second caller coming -- a guard or a "call exactly once" KDoc; the KDocs above `mh`/`mhResume` still say "package-private in Java".
Task 4: minor (deferred): ProgramUnit.buildTable's post-constructor writes to hasExitHandler/isThunk/source fields get no happens-before from bodyReady (pre-existing, gone with Task 5's shells); ArgsExpectation's invoke road now pays one volatile read per invoke (plan-mandated, expected in the noise); racingReadersFillOnce is probabilistic; isThunk/sourceLineDelta/sourceSection* getters untested; StaticCodeInfoLazyTest.kt:32's bare getter reads want a comment.
Task 4: watch (for Task 7): ruling 10 narrows one match -- a frame running a freshcoderef'd outer (its StaticCodeInfo is a clone) no longer matches by shared mh+compUnit; RakOps already behaved this way; the Rakudo build in Task 7 is the first real exercise.
Task 4: complete (commits nqp d57d5aad4..94d74785f, review clean)
Ruling (for Task 5, from Task 4's concern 2): a StaticBodySource.fill that reads a body GETTER recurses unboundedly (the monitor is reentrant, bodyReady still false). The plan's ProgramUnit.BodySource applies the block's static lexical values inside fill through `sci.oTryGetLexicalIdx` and the `oLexStatic` getter -- that is exactly that recursion. Task 5 therefore adds two `internal` raw accessors to StaticCodeInfo that never call ensureBody -- `internal fun rawOLexicalIdx(name: String): Int` (the body of oTryGetLexicalIdx after its ensureBody line, shared by both) and `internal fun rawSetOLexStatic(idx: Int, value: SixModelObject?, flags: Byte)` -- and ProgramUnit.applyLexValues uses only those, so the values are applied inside fill, before bodyReady is published (no window in which a reader sees a ready body with unapplied values). The drain after deserialization uses the same raw accessors on blocks whose fill has completed. Costs if wrong: a StackOverflowError at unit load, caught by Task 5's own tests and the nqp suite.
Fact (for Task 5): SerializationReader sets `orig.order(LITTLE_ENDIAN)` itself (SerializationReader.kt:96) and never calls array()/hasArray(), so the mapped `unit.serialized` slice is handed over as is.
BASE before Task 5: rakudo 107eca63a3, nqp 94d74785f.
Task 5: complete (nqp 009103869; runtime tests green; nqp suite 155 green in 188 s; stage lines seen: open-store, transcode-v1, shells, deserialize-program, static-lex-drain, load-block)
Task 5: red round 1 -- t/nqp/123-unit-artifact.t subtests 2-3 (format assertions only; subtests 4-8, the compile-to-jar-and-use road, passed): subtest 2 asserted unit.meta, which v2 replaces with unit.index; subtest 3 scanned the whole jar decoded as latin-1 for ".class" and now sees the string literal ModuleLoader.class in the STORED (uncompressed) program text. Both rewritten to parse the zip local-header chain from the uint8 buffer; 8/8 after.
Task 5: deviation from the brief (wants a ruling) -- a static lexical row naming a qbid that is a gap or beyond the block table is DROPPED, not a dieInternal: QAST::Compiler.cuid_to_qbid allocates a qbid for every cuid in %*BLOCK_LEX_VALUES, including blocks never compiled into the unit (the gap ProgramUnitTest documents), and v1 dropped those rows silently at apply time. RecordReader and UnitZip.toImage drop them where the grouping happens, with an NQP_CODE_WHY-gated line naming unit, qbid and count.
Task 5: ./rakudo-j not smoke-tested -- two independent pre-existing stalenesses, both Task 7 rebuild: (1) NoClassDefFoundError kotlinx/serialization/KSerializer, the runner NQP_JARS predating Task 2 (the jar is in nqp/build/jvm/share/runtime and in nqp jvmconfig.properties; only the generated runner is stale); (2) with that classpath corrected, "Missing or wrong version of dependency .../nqp/build/jvm/stage2/NQPHLL.nqp" -- rakudo.jar/rakudo-runtime.jar/blib jars are 2026-09-14, the nqp stage jars 2026-09-15 15:02. nqp/nqp-j (non-gradle) is separately dead: it points at nqp/gen/jvm/share, absent in this worktree.
Task 5: also carried Task 4 review items -- finishBody() idempotent (private bodyFinished, "runs once" KDoc) and the mh/mhResume KDocs reworded; EvalResult.record retyped to UnitStore (not in the brief file list); SerializationContextTest folded onto ProgramUnitTestSupport so one fixture exists, not three; UnitLoadStats needed no change (it enumerates no stage names).
Task 5: review (Opus): spec compliant, quality approved; reentrancy fix verified on both sides (every write in fill is a barrier-free setter; the raw accessors call no ensureBody); lock order fill sci->unit, drain releases the unit monitor before applying; transcoder carries every v1 field; intOr's catch is exact (RuntimeException with the "No such attribute" prefix only); stamp 20:50 +02:00 and the single trailer verified by the controller. Round 1 of the nqp suite had one red, t/nqp/123-unit-artifact.t subtests 2-3 (v1 entry-name and ".class" substring assertions), rewritten to walk the zip's local-header chain; round 2 155 green in 188 s.
Ruling: a static lexical row naming a gap qbid is dropped at grouping time (NQP_CODE_WHY-gated line), not a hard error -- v1's applyStaticLexValues silently skipped such rows (`table.getOrNull(qbid) ?: continue`) and the compiler allocates qbids for %*BLOCK_LEX_VALUES cuids it never compiles, so the plan's dieInternal would have broken the build -- costs if wrong: nothing (behaviour-preserving).
Ruling: t/nqp/123-unit-artifact.t subtests 2-3 assert the v2 shape (first entry unit.index, no .class entries, by walking local headers) -- the meaning of the originals, strictly stronger -- costs if wrong: nothing.
Ruling: applyLexValues throws IllegalStateException (no ThreadContext inside a fill; UnitLoader wraps load-time failures in dieInternal); a fill forced mid-execution surfaces the error at the block's first invocation rather than at load -- accepted, plan-mandated -- costs if wrong: an error message shape.
Ruling: UnitImageWriter's require() checks on programIndex/outerQbid fire at write time for in-memory units too (the same invariants v1 enforced at read) -- accepted -- costs if wrong: an in-memory unit with a corrupt table dies at write instead of later.
Task 5: minor (deferred -> folded into Task 6's dispatch): RecordReader.intOr throws-and-catches one RuntimeException per block per compile under a stage0 compiler (with a full attribute-dump message built each time) -- remember the absent (type, name) pair beside the hints cache; the NQP_CODE_WHY drop line at RecordReader.kt:140-142 indexes table[q] without a q < 0 guard.
Task 5: minor (deferred): ProgramUnit.gc could be @Volatile (safe today through lexValuesReady and the unit monitor); UnitZip.toImage has no unit test (window-only, deleted in Task 9); the report overstated toImage's diagnostic (it drops gap rows implicitly, prints nothing); 123-unit-artifact.t's header walk would go vacuous on a deflated entry (a `5 entries` assertion would pin it); UnitLoader.isUnitFile(fn, shared) ignores `shared`; getCallSites() allocates per call.
Task 5: complete (commits nqp 94d74785f..009103869, review clean)
BASE before Task 6: rakudo 107eca63a3, nqp 009103869.
Ruling (Task 6 dispatch): since Task 5 every unit is a ProgramUnit, so `cu is ProgramUnit` alone would give in-memory units an identity; the plan's ruling 6 (no identity for in-memory units: fresh sha1 ids, identical texts must share a root) is kept by testing the store's name -- `ProgramUnit.isStoreBacked()` = the store name does not start with "<" ("<memory:...>" for UnitWriter.store, "<buffer>" for loadbytecodebuffer; a file path never starts with "<") -- costs if wrong: a program of an in-memory unit keyed by identity would be parsed per compile instead of shared by text (memory growth in a long server sweep), never a wrong result.
Ruling (Task 6 dispatch): `$*BREC` is read in the encoder through nqp::getlexdyn (null when unbound) rather than the plan's `nqp::defined($*BREC)` (an unbound dynamic throws) -- costs if wrong: nothing.
Task 6: complete (nqp a0c850bf6; stats sites=3011 anon=4; nqp suite 155 green in 587 s; the encoder edit waits for Task 7's clean build)
Task 6: implementer DONE_WITH_CONCERNS (nqp a0c850bf6): stats line `hits=25800 misses=1877 sites=3011 anon=4` on nqp-j -e; 614 site-check lines; 125-dispatch-stats.t 1..6 ok; :nqp-runtime:test 45/45; nqp suite 155 green in **587 s** (sweep wall 16:28:43 -> 16:38:31; log b-task6-nqp.log).
Gate clock table so far (nqp suite, 155 files, one warm 6 GB server): Phase A close 186 s / 200 s; after Task 4 189 s; after Task 5 193 s; after Task 6 587 s. USER (2026-09-15): timings are reported and recorded for every gate run from here on ([[report-gate-timings]]).
Task 6: the 587 s is unexplained: the implementer reports no other load (three idle daemons), no NQP_* env leak, the engine jar rebuilt and synced, a fresh sweep-started server, and nothing in the diff that runs per block entry or per materialize hit; the one behavioural change it names is that two store-backed units with identical program text no longer share a compiled target. Controller action: the reviewer reads the diff for per-run cost as its first named risk, and ONE diagnostic re-run of the suite on the same jars (log b-task6-nqp-rerun.log) decides real vs confounded before Task 7 builds on it -- a diagnosis of a 3x gate clock, not a benchmark re-take.
Task 6: diagnostic re-run of the nqp suite on the same jars (log b-task6-nqp-rerun.log, started 16:44:34): 155 green in 192 s, EXIT=0. The 587 s run spanned a laptop suspend (user, 2026-09-15); Task 6 is clock-neutral (189 / 193 / 192 s). The gate-clock table row for Task 6 is 192 s.
Task 6: review (Opus): spec compliant, quality approved; risk 1 (the suite clock) traced: materialize short-circuits on engineTarget per run, the changed branch decodes the text only on a miss (cheaper than before), Source names stable across runs, put/get symmetric through one parsedKey, regex names ("rx" or the rule name) never parse as identities -- the diff cannot explain 587 s; consistent with the suspend and the 192 s re-run. Stamp 20:55 +02:00 and the single trailer verified by the controller.
Task 6: minor (deferred): ProgramIdentity.parse accepts a negative index (`foo#-3`); SiteIdentity.site()/the value class are unexercised until Phase C; intOr allocates a Pair before the `absent` test; isStoreBacked is an extension (plan-mandated shape); ProgramUnit's KDoc says "two" in-memory makers where a third `<nested:...>` store name exists on the write side only (nested units inherit the parent's name at read time, so the test is right); identity keys are namespaced by unitId alone -- content sha1 for every artifact but the name-addressed `nqp`; a debug-gated assert is a Phase C candidate.
Task 6: complete (commits nqp 009103869..a0c850bf6, review clean; nqp suite 192 s)
BASE before Task 7: rakudo 107eca63a3, nqp a0c850bf6.
Task 7: clean buildJvm 219 s (BUILD SUCCESSFUL; stage2 nqp.jar = 5 unit.* entries, unit.index first, unit.dispatch present at 0 bytes); nqp-j smoke `say(1)` -> 1, 9 open-store lines, zero transcode-v1 lines (stage0 v1 is read only by the build).
Task 7: :nqp-runtime:test 1 s (FROM-CACHE, 45 tests / 0 failures / 0 errors over 7 classes).
Task 7: nqp suite 155 green in 200 s (sweep wall 201 s, one warm server, zero 'not ok', EXIT=0).
Task 7: red round 1 -- `make clean && make` died at blib/CORE.c.setting.jar after 539 s with "Lexical '$*STACK-ID' not found in guard-type-concreteness (src/vm/moar/dispatchers.nqp:55)", a sub that reads no lexical. NQP_VERBOSE_EXCEPTIONS put the throw in NqpOps.bindlexWalk under an UNCACHED bytecode node reached from Dispatch.record: the frame was running another unit's program. Root cause = Task 6's program identity key "<unit id>#<program index>": a unit id is author-supplied (nqp --javaclass) and Rakudo's build gives rakudo.jar, v6c.jar, v6d.jar, v6e.jar and rakudo-debug.jar the SAME id "perl6" (Makefile J_NQP_FLAGS_EXTRA = --javaclass=perl6), so the CORE.c compile, which loads rakudo.jar and v6c.jar together, had one unit's programs answering for the other's in CodeEngines.programs and NqpLanguage.PARSED. Confirmed by listing unit ids per open store: `perl6` twice, every other artifact a distinct sha1. Fix (nqp 46db78f61): ProgramUnit.identityNamespace() = store name + "!" + unit id -- neither alone is unique (a nested unit inherits the parent's store name; two files can share an id), the pair repeating means the same artifact re-opened (eval-server sharing preserved); ProgramIdentity's field renamed unitId -> namespace, parse unchanged (it already split at the last '#').
Task 7: note (for the controller) -- the Rakudo build's `--javaclass=perl6` on five artifacts is the underlying invariant violation and is left as is: the nqp-side namespace makes it harmless, and changing the Makefile template would churn Configure for no behaviour gain.
Task 7: red round 1, false lead recorded -- a reduced repro (`rakudo-j-build --setting=NULL.c` on a one-line file) fails afterwards with "Method 'elems' not found for invocant of class 'Array'" in RakuAST::CompUnit.check. That is NOT a regression: check does `$!pod-content.elems` unconditionally and Array gets `elems` only from src/core.c/Array.rakumod, which a NULL.c compile of a file that is not CORE.c never parses. Only the real CORE.c target exercises that path.
Task 7: gate clocks so far: clean buildJvm 219 s (stage1/stage2 written v2 through the transcoder); nqp suite on the window build 155 green in 200 s (the stage0-rule gate: green); Configure.pl + make clean && make round 1 FAILED at 539 s in the CORE.c compile: "Lexical '$*STACK-ID' not found in guard-type-concreteness (dispatchers.nqp:55)" -- a sub that reads no lexical: the frame ran another unit's program.
Task 7: fix round 1 (implementer, nqp 46db78f61 "a program identity names its store, not just its unit id"): Task 6 keyed a store-backed program by "<unit id>#<index>" assuming a unit id identifies an artifact; Rakudo's Makefile passes --javaclass=perl6 to rakudo.jar, v6c.jar, v6d.jar, v6e.jar and rakudo-debug.jar alike, so with rakudo.jar and v6c.jar in one process program N of whichever loaded first answered for the other's. The namespace is now store name + unit id (ProgramUnit.identityNamespace); ProgramIdentity's field is renamed to `namespace`. Also nqp 04d7d9ee9 = the unit-check line (Step 5).
Ruling: the fix stands. The store name is a file path, so a site identity is no longer a pure function of the artifact's content across machines -- but Phase C looks a slot up through the site's own unit (program index, ordinal), never through the string across processes, so persistence is unaffected; the eval server re-opening one path per run still shares one parsed root. Costs if wrong: Phase C must not key anything cross-process by the identity string (recorded for its plan).
Task 7: deferred (hygiene, Phase C or the close): the Makefile's shared `--javaclass=perl6` for five units is a latent trap for any per-unit keying; distinct names per unit would remove it.
Task 7: Configure.pl --backends=jvm --gen-nqp 3 s; make clean && make 854 s (round 2, green), CORE.c (window build) 283 s (log b-window-make2.log, "Compiling blib/CORE.c.setting.jar" at 522 s -> "Generating gen/jvm/BOOTSTRAP/v6d.nqp" at 805 s); ./rakudo-j -e 'say 1 + 1' -> 2.
Task 7: t/01-sanity 25/25 in 58 s (Files=25, Tests=303, Result: PASS).
Task 7: sizes (v2, every entry STORED/uncompressed) -- CORE.c.setting.jar 56067438 (was 5892366): index 1267299, records 5426077, programs 21204318, serialized 28159968, dispatch 0, plus 4 nested units at 1568 each; v6c.jar 11269824 (was 1299129): index 404944, records 552603, programs 6117912, serialized 4193837, dispatch 0; stage2 nqp.jar 1145720 (was 139990): index 41102, records 61969, programs 693906, serialized 348215, dispatch 0; rakudo.jar 22858: index 921, records 1286, programs 12919, serialized 7204, dispatch 0. The 8-10x growth over the v1 numbers is the stored (mmap-able) format: nothing is deflated any more.
Task 7: site/unit check on ./rakudo-j -e 'say 1' -- 2598 site-check programs numbered, max ordinals 1060; 26 unit-check lines (unit-check perl6 appears THREE times: rakudo.jar programs=11 slots=43, v6c.jar programs=4682 slots=21829, and a 7-program unit -- the collision of round 1, now harmless); no unit numbers more ordinals than it stores slots (per-unit-id sums, worst case 2D8CD... 1999/2730).
Task 7: the brief's Step 5 one-liner has a typo -- $*ENV<CLAUDE_JOB_DIR> must be %*ENV<CLAUDE_JOB_DIR>; with that it prints "2598 programs numbered; max ordinals 1060".
Task 7: make round 2 (after nqp 46db78f61): Configure.pl + make clean && make 854 s, EXIT=0, all settings; CORE.c 283 s (log window [522s]..[805s]; Phase A's makes had it near 300 s). t/02-rakudo sweep started 17:46:35, 307 files (the plan said 306), one 6 GB server.
Task 7: t/02-rakudo sweep attempt 1 was killed by the harness's low-memory guard seconds after the server started (1 server x 6g heap + 3g off-heap of a 21g budget); gradle daemons stopped (MemAvailable 24 -> 27g) and re-run at --heap=5. Not a test failure; nothing was measured.
Task 7: t/02-rakudo sweep attempt 2 (implementer's choice after the guard refusal): --heap=4 --chunk=50, 7 chunks of 50 over 307 files, i.e. a fresh server per 50 files; chunks 1-2 reported "FAIL *** a file produced no TAP ***" at [758s] and [1069s] -- a verdict the sweep gives whenever any file in the chunk yields no TAP, which several baseline-red files do; not evidence of a regression by itself. Ruling: this sweep is the correctness gate before stage0 regeneration (new-red=0 against the 24-file baseline decides), NOT a warm-clock measurement, so the chunking is accepted for this run; its wall time is recorded but not comparable to the single-server 3200 s series -- costs if wrong: nothing, the clock was never a Phase B deliverable (the b row is --warm=proxy).
Task 7: MemAvailable at 18:25 was 23.3 GB of 32 GB (no swap); the eval server runs in a systemd scope capped at heap+3 GB, and the launcher refuses (exit 75) when need + the live servers' growth room exceeds MemAvailable -- attempt 1's refusal came seconds after start while the gradle daemons (about 3.5 GB) and the make's JVMs were still resident.
Task 7: t/02-rakudo sweep -- the plan's shape (ONE server, --chunk=307) could not be run: four background attempts (watched-run and without, heap 6/5/4/3) were each killed by the Claude Code harness's low-memory guard seconds after the server banner, with MemAvailable 25-27 GB and nothing else running; a 3-file background sweep survives, so the guard fires on the 307-file run, not on the heap. DEVIATION (recorded, wants a ruling): the gate was run as `--jobs=1 --heap=4 --chunk=50 t/02-rakudo`, i.e. 7 sequential servers, 307 files in 3455 s. The wall time is therefore NOT comparable to a one-server sweep; the red list is.
Task 7: m7-rig --parse-sweep -> `warm=3455 red=21 new-red=-` (an empty new list): NEW-RED=0 against the 24-file baseline. Red: 03-cmp-ok, 03-corekeys{,-6c,-6d,-6e}, 04-settingkeys-6c, 04-settingkeys-6e, 21-begin-time-compile-sub, begin-called-block-routine, compiler-frontend-id, constant-anon-var-value, custom-declarator-naming, long-int-literal, make-regex-frame, m-flag-module-spec, native-return-coercion, parse-target-match-tree, regex-interpolation-backtrack, regex-interpolation-fold-length, str-raku-prepend, try-statement-backtrace-frame (21). Green this run though listed in the baseline: 15-gh_1202.t, 16-begin-time-eval.t, native-argument-snapshot.t. 99-misc.t is a TODO-pass, not a red.
Task 7: t/02-rakudo sweep attempt 2 done: 307 files in 3455 s across 7 servers (chunked; not comparable to the single-server series), 21 red, ALL in the 24-file baseline, new-red=0 (controller's diff by name from the harness lines; the rig's --parse-sweep printed red=21 new-red=- because the chunked log carries no Wstat lines). Newly green against the baseline: 15-gh_1202.t, 16-begin-time-eval.t, native-argument-snapshot.t. Every "no TAP" file (21-begin-time-compile-sub, begin-called-block-routine, constant-anon-var-value, custom-declarator-naming, make-regex-frame, parse-target-match-tree) is a baseline red. Gate PASSED: stage0 may be regenerated from this build (after the post-fix nqp suite re-run, started 19:03).
Task 7: minor (deferred): m7-rig.raku --parse-sweep cannot name red files from a chunked sweep log (no Wstat lines); it reads the harness's per-file lines only for a single-chunk run.
Task 7: nqp suite re-run on the FINAL window runtime (after the identity fix and the unit-check line): 155 green in 191 s, zero 'not ok', EXIT=0 (log b-window-nqp-suite2.log). The 200 s run earlier in the task was on the pre-fix runtime; this is the gate.
Task 7: complete (window build: nqp 46db78f61 rakudo 107eca63a3; clean buildJvm 219 s; :nqp-runtime:test 45/45 in 1 s; nqp suite 155 green in 191 s; Configure.pl 3 s; make clean+make 854 s, CORE.c 283 s; t/01-sanity 25/25 in 58 s; t/02-rakudo new-red=0 in 3455 s (7 servers, see the deviation above); sizes: CORE.c 56067438 (was 5892366), v6c 11269824 (was 1299129), nqp.jar 1145720 (was 139990), rakudo.jar 22858)
Task 7: implementer DONE_WITH_CONCERNS. Gate table: clean buildJvm 219 s | :nqp-runtime:test 45/45 in 1 s | nqp suite 155 green in 200 s (pre-fix) and 191 s (post-fix, final runtime) | Configure 3 s | make clean+make 854 s (CORE.c 283 s) | t/01-sanity 25/25 (303 tests) in 58 s | t/02-rakudo red=21 new-red=0 in 3455 s (7 chunked servers) | sizes: CORE.c.setting.jar 56067438 (was 5892366), v6c 11269824 (was 1299129), nqp.jar 1145720 (was 139990), rakudo.jar 22858 (was 7537).
Task 7: concern (implementer): four one-server --chunk=307 sweep attempts were each killed seconds after the server started although MemAvailable was 25-27 GB; the chunked --heap=4 run succeeded. Unexplained; the single-server sweep is needed again at the milestone close (whole t/), so this goes to Phase C's inbox / the final review as an open question about the launcher's guard or the systemd MemoryMax scope.
Task 7: concern (implementer): v2 artifacts are 8-10x v1 on disk (every entry stored, both LZ4 layers gone); v1's "uncompressed" column was itself LZ4 output, so the plan's 13 MB estimate for CORE.c was wrong by 4x. USER DECISION requested before Task 8 (stage0 is 9 jars committed to git: v2 stage0 would add about 10 MB of binaries to history per regeneration).
Task 7: minor (deferred): the plan's Step 5 one-liner reads $*ENV where Raku wants %*ENV.
USER DECISION (2026-09-15, before Task 8): proceed as planned -- regenerate stage0 now, measure the cold clocks in Task 10, compression of stored entries goes to Phase C's inbox -- BUT do not commit ANY jar of any type to git until the user says so. Task 8's stage0 jars stay uncommitted working-tree changes; Task 9 builds from that working tree; the close's push carries a v1 stage0 the runtime can no longer read (stated at the handoff). Memory: [[no-jar-commits-until-told]].
Task 7: review (Opus): approved; identity fix holds at all five cases (key shapes agree, in-memory units still anonymous, nested distinct from parent, same path per run shares a root, the string is never persisted or compared across processes); stamps 21:00/21:05 and single trailers verified. Four Important findings -> fix round 1: (1) the report presents `new-red=-` as evidence (it is the rig's "no names" output; the pass is the controller's by-name diff); (2) unit-check prints the bare unit id while site-check prints the namespaced identity, so the two cannot be joined for the three `perl6` units -- Ruling: unit-check prints the namespace too; (3) SiteIdentity.kt's KDoc still says "known by across processes"; (4) no regression test for the namespace (ProgramUnitTestSupport's store name as a parameter: null for "<test>", "x.jar!unit-x" for a file-like name, parent != nested). Minor (deferred): NqpLanguage.java's PARSED/parsedKey javadoc still says "<unit>#<index>"; :nqp-runtime:test's last real run predates the fix (a FROM-CACHE run); "!" as the namespace separator is theoretical ambiguity; the report's sizes table lacks rakudo.jar's base; the watch list of amendment 5 was answered implicitly (green build) rather than named.
Task 7: fix round 1 of 5 (nqp 35b28f9e3) -- review findings 1-4: the report's new-red paragraph corrected (the rig parsed no names from the chunked log; the pass is the controller's by-name diff of 21 red files against the baseline, 3 baseline reds green) and rakudo.jar's base size (7537) added; unit-check now prints identityNamespace() so it joins site-check by prefix ("unit-check ./rakudo.jar!perl6 programs=11 slots=43" / "unit-check blib/Perl6/BOOTSTRAP/v6c.jar!perl6 programs=4682 slots=21829" / "unit-check blib/Perl6/BOOTSTRAP/v6d.jar!perl6 programs=7 slots=8"); SiteIdentity's and NqpLanguage's "across processes" / "<unit>#<index>" docs corrected (the namespace is this process's path to the store; Phase C resolves a slot through the site's own unit, program index and ordinal); ProgramUnitTest.identityNamespaceNamesBothTheStoreAndTheUnit added (null for "<test>", "x.jar!unit-x" for a file name, a nested unit's "x.jar!inner" != its parent's). :nqp-runtime:test --rerun EXECUTED: tests=46 skipped=0 failures=0 errors=0; jars synced; the smoke prints 26 unit-check lines.
Task 7: fix round 1/5 (4 addressed, 0 open -- report's new-red claim, unit-check namespaced, KDocs, namespace regression test; commit nqp 35b28f9e3; :nqp-runtime:test 46/46 executed in 11 s; unit-check 26 lines)
Task 7: complete (commits nqp a0c850bf6..35b28f9e3, review clean after round 1; clocks: buildJvm 219 s, nqp suite 200/191 s, make 854 s, CORE.c 283 s, t/01-sanity 58 s, t/02 3455 s chunked new-red=0)
BASE before Task 8: rakudo 107eca63a3, nqp 35b28f9e3. Stage0 jars will NOT be committed (user rule).
Task 8: jBootstrapFiles 216 s -- stage2's nine jars copied over src/vm/jvm/stage0; `git status --short` in the nqp tree lists exactly nine ` M src/vm/jvm/stage0/*.jar` and nothing else. Every new stage0 jar has a unit.index entry (9 lines of 1; the v1 ones had 0). Layout v1 -> v2: ModuleLoader.jar was unit.meta/unit.programs/unit.serialized.lz4 (5404 bytes), is now unit.index/unit.records/unit.programs/unit.serialized/unit.dispatch (25237 bytes, dispatch 0 bytes). Per user rule these jars stay UNCOMMITTED.
Task 8: clean buildJvm 229 s (BUILD SUCCESSFUL in 3m49s), run with NQP_UNIT_LOAD_STATS=1 exported for the whole build: 18 "unit-load: stats on" markers (18 JVMs), 0 transcode-v1 lines. The new stage0 is read natively (unit-load 1 nqp.jar open-store bytes=1145720).
Task 8: :nqp-runtime:test --rerun-tasks 25 s, 7 classes, tests=46 skipped=0 failures=0 errors=0.
Task 8: transcode check (explicit, decision 3): NQP_UNIT_LOAD_STATS=1 ./nqp/gradlew -p nqp :stage1CompileNqp --rerun -> BUILD SUCCESSFUL in 14 s, 1 positive marker, 16 open-store lines, transcode-v1 lines 0.
Task 8: nqp suite 155 green in 192 s (one 6 GB server, --chunk=*, EXIT=0, zero 'not ok'); daemons stopped first, MemAvailable 27 GB, no launcher refusal.
Task 8: Configure.pl --backends=jvm --gen-nqp 7 s.
Task 8: make clean && make 861 s (EXIT=0), CORE.c 280 s (log b-stage0-make.log, "Compiling blib/CORE.c.setting.jar" at 530 s -> "Generating gen/jvm/BOOTSTRAP/v6d.nqp" at 810 s); ./rakudo-j -e 'say 1 + 1' -> 2. Task 7's window build was 854 s / CORE.c 283 s: the v2 stage0 changes neither clock (within noise).
Task 8: t/01-sanity 25/25 in 59 s (Files=25, Tests=303, Result: PASS).
Task 8: stage0 sizes v1 -> v2 (bytes): ModuleLoader 5404 -> 25237, NQPCORE.setting 33022 -> 205064, NQPHLL 81673 -> 572756, nqp 139798 -> 1145720, nqpmo 28861 -> 211969, NQPP6QRegex 72054 -> 598108, QAST 120330 -> 750084, QASTNode 32506 -> 253443, QRegex 43413 -> 285754; total 557061 -> 4048135 (7.3x, the stored/mmap-able format, as in Task 7).
Task 8: Step 4 (commit stage0) CANCELLED by user rule -- no jar is committed. nqp HEAD is unchanged at 35b28f9e3; `git status --short` in the nqp tree lists exactly the nine ` M src/vm/jvm/stage0/*.jar` lines, nothing staged. Task 9 builds from this working tree.
Task 8: complete (stage0 = 9 v2 jars, UNCOMMITTED by user rule; jBootstrapFiles 216 s; clean buildJvm 229 s; transcode-v1 lines 0; nqp suite 155 green in 192 s; make clean+make 861 s, CORE.c 280 s; t/01-sanity 25/25 in 59 s)
Task 8: implementer DONE, no commits. Gates: jBootstrapFiles 216 s (nine jars, unit.index in each, unit.dispatch 0 bytes) | clean buildJvm 229 s, transcode-v1 lines 0 | :nqp-runtime:test 46/46 in 25 s | nqp suite 155 green in 192 s (one 6 GB server, no launcher refusal) | Configure 7 s | make clean+make 861 s, CORE.c 280 s | t/01-sanity 25/25 (303 tests) in 59 s. Stage0 557061 -> 4048135 bytes (7.3x). nqp status: exactly nine ` M src/vm/jvm/stage0/*.jar`, nothing staged.
Ruling: Task 8 produced no diff, so its review is the controller's check of the gate logs (buildJvm, suite, make, sanity verdicts and clocks read directly from the watched-run logs during the run) -- no reviewer dispatched -- costs if wrong: nothing (no code changed).
Task 8: watch (for Task 9 and the close): the v2 stage0 exists only in the working tree; any `git checkout`/`git clean`/`git stash` in the nqp tree reverts it to v1, which Task 9's runtime cannot read; the close's push carries the v1 stage0 (user rule, stated at the handoff). Also: rakudo `git status` in this worktree needs --ignore-submodules=all (nqp-configure quirk).
Task 8: complete (no commits; stage0 regenerated in the working tree; gates green: buildJvm 229 s, suite 192 s, make 861 s / CORE.c 280 s, sanity 59 s)
BASE before Task 9: rakudo 107eca63a3, nqp 35b28f9e3 (+ nine uncommitted stage0 jars).
Task 9: deletions -- UnitFormat.kt, UnitRecord.kt, UnitZip.kt, UnitFormatTest.kt (git rm, nqp tree); UnitLoader.kt loses the isUnitFile(fn, shared) overload (its one caller, loadApp, takes the one-arg form), the UnitZip.isUnit disjuncts (isUnitFile, load(tc, ByteArray)), transcodeV1 and both openStore fallbacks -- a non-v2 file is now UnitStore.open's IllegalStateException naming the file ("no unit.index entry" / "bad magic" / "version N, this runtime reads 2"); UnitImage.kt KDoc drops "or a transcoded v1 record". UnitLoadStats enumerates no stage names, so nothing to drop there.
Task 9: LZ4 grep (nqp/src/vm/jvm/runtime, nqp/nqp-truffle/src, src/vm/jvm/runtime, nqp/buildSrc): the only hits were UnitFormat.kt:6-8,20-21,108-122 -- removed org.lz4:lz4-java:1.8.0 from NqpDeps.thirdParty and "lz4-java" from moduleOrder. The legacy Perl path (nqp/tools/lib/NQP/Config/NQP.pm:249, nqp/tools/templates/jvm/Makefile.in:15,25,26,56, nqp-j.in, install-jvm-runner.pl.in) still names the VENDORED nqp/3rdparty/lz4 jar; it is independent of NqpDeps and out of this task's scope.
Task 9: clean buildJvm 255 s (BUILD SUCCESSFUL in 4m 13s), rc=0.
Task 9: :nqp-runtime:test --rerun-tasks 10 s, 6 classes (UnitCodecTest, UnitStoreTest, ProgramUnitTest, StaticCodeInfoLazyTest, SerializationContextTest, GraphemeCursorTest), tests=36 skipped=0 failures=0 errors=0 (was 46/7; UnitFormatTest's 10 left with the file).
Task 9: nqp suite 155 green in 197 s (one 6 GB server, --chunk=*, EXIT=0 verdict=ok, zero 'not ok'); daemons stopped first, MemAvailable 24.8 GB, no launcher refusal.
Task 9: Configure.pl --backends=jvm --gen-nqp 7 s. lz4 propagation confirmed: nqp/build/jvm/share/lib/jvmconfig.properties runtime.jars is now fastutil, jline, kotlin-stdlib, kotlinx-serialization-core-jvm, annotations, nqp-runtime, nqp.jar -- no lz4-java; the generated Makefile names no lz4 either.
Task 9: make clean && make 863 s (EXIT=0 verdict=ok elapsed=862s), CORE.c 281 s (log b-v1gone-make.log, "Compiling blib/CORE.c.setting.jar" at 530 s -> "Generating gen/jvm/BOOTSTRAP/v6d.nqp" at 811 s); RAKUDO_RAKUAST=1 ./rakudo-j -e 'say 1 + 1' -> 2. Task 8's two-reader build was 861 s / CORE.c 280 s: dropping the v1 reader changes neither clock (within noise, as expected -- the window never transcoded).
Task 9: t/01-sanity 25/25 in 60 s (Files=25, Tests=303, Result: PASS). No precomp-cache failure occurred; nothing was cleared.
Task 9: commit nqp 318558c2d (stamp 2026-09-15T21:20:00+02:00, author and committer; amended from fc315e5cf for the trailer, see the ruling below). 7 files, 8 insertions, 528 deletions; `git show --stat HEAD` lists no .jar; `git status --short` in the nqp tree still lists exactly the nine ` M src/vm/jvm/stage0/*.jar`, nothing else.
Task 9: RULED (controller) -- the "Claude Fable 5.1" trailer is a PHASE CONVENTION naming the directing session, not the implementer (implementers run on Opus by the user's model policy). The implementer had first written Opus 5, read it as a model-attribution claim, and flagged it; on the ruling the commit was amended (fc315e5cf -> 318558c2d) to the single trailer "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>", message and both 2026-09-15T21:20:00+02:00 dates unchanged, still no .jar in the commit and the nine stage0 jars still unstaged. Note for later briefs: say that the trailer names the directing session, so it does not read as a misattribution.
Task 9: minor (deferred): the legacy Perl configure path still carries lz4 (NQP/Config/NQP.pm:249, tools/templates/jvm/Makefile.in:15,25,26,56, nqp-j.in:35, nqp-j.windows:4, install-jvm-runner.pl.in:24) against the vendored nqp/3rdparty/lz4 jar; harmless (that classpath entry is now dead weight) and out of scope, but it is the last lz4 in either tree. RecordReader.kt:35's "transition window" KDoc is about a stage0 compiler whose QAST::BlockRecord predates a field -- a different, still-live transition, deliberately untouched.
Task 9: complete (nqp 318558c2d; one reader; lz4 removed; clean buildJvm 255 s; nqp suite 155 green in 197 s; make clean+make 863 s, CORE.c 281 s; t/01-sanity 25/25 in 60 s)
Task 9: implementer DONE (nqp fc315e5cf, amended to 318558c2d for the phase trailer after a ruling: the trailer names the directing session, Claude Fable 5.1, on every commit of the phase regardless of the implementer's model). Gates: clean buildJvm 255 s | :nqp-runtime:test 36/36 in 10 s (UnitFormatTest gone) | nqp suite 155 green in 197 s (one 6 GB server) | Configure 7 s | make clean+make 863 s, CORE.c 281 s | t/01-sanity 25/25 (303 tests) in 60 s. Deleted UnitFormat/UnitRecord/UnitZip/UnitFormatTest (-528 lines); LZ4 removed from NqpDeps (used only by UnitFormat.kt); UnitLoader without transcoder, v1 sniffs, or the (fn, shared) overload (closes a Task 5 minor). No jar in the commit; nine stage0 jars still unstaged.
Task 9: minor (deferred): the legacy Perl build path (tools/lib/NQP/Config/NQP.pm:249, Makefile.in, nqp-j.in, nqp-j.windows, install-jvm-runner.pl.in) still references the vendored 3rdparty/lz4 jar -- dead weight, out of scope; a bad `-app` path now reports the store's IllegalStateException (names the file) instead of "is not a unit artifact".
Gate clock table (155-file nqp suite / make / CORE.c / t/01-sanity): Task 7 window 200-191 s / 854 s / 283 s / 58 s; Task 8 stage0 192 s / 861 s / 280 s / 59 s; Task 9 v1-gone 197 s / 863 s / 281 s / 60 s. Flat, as expected once stage0 was v2.
Ruling (Task 10 dispatch): the handoff rebase runs for rakudo (origin/main) as usual; for nqp the nine uncommitted stage0 jars block a plain rebase, so: fetch upstream; if upstream/main has no new commits (the case at every handoff so far), skip the rebase and record it; if it has, rebase with --autostash (the jars are binary and upstream nqp has no JVM work, so the re-apply cannot conflict) and verify the nine ` M` lines afterwards -- costs if wrong: a stash entry left behind on a failed rebase, recoverable from `git stash list`.
Ruling (Task 10 dispatch): the pushes go to the ab5tract fork branches as the standing handoff rule requires; the nqp push carries the committed v1 stage0 that the pushed runtime can no longer read -- stated in the handoff and in the memory until the user lifts the no-jar rule.
Task 9: review (Opus): spec compliant, quality approved; zero dangling references (four greps), error paths still name the file through dieInternal, orderKey covers every remaining jar, gate logs and test XMLs on disk match the report. Minor (deferred): NqpDeps.kt's KDoc still says the list mirrors the Makefile.in/nqp-j.in templates, which still name lz4; nqp/Makefile (legacy) still defines LZ4 (the ledger line about "the generated Makefile" meant rakudo's); no unit test for UnitLoader.load(tc, ByteArray)'s rejection path.
Task 9: complete (commits nqp 35b28f9e3..318558c2d, review clean; clocks buildJvm 255 s, suite 197 s, make 863 s / CORE.c 281 s, sanity 60 s)
BASE before Task 10: rakudo 107eca63a3, nqp 318558c2d (+ nine uncommitted stage0 jars).
Task 10: rig row (70 s wall, all three positive markers, no retry needed) -- `| b | 107eca63a3 | 318558c2d | 2.461 | 1.160 | 5667 | 100711 | 50/sanity | none | |`. Against a6 (2.504 / 1.192 / 5661 / 100697 / 51 s): every number inside the series' own spread (rakudo walls 2.50 2.53 2.46 2.67 2.61; nqp 1.23 1.17 1.16 1.22 1.25), so Phase B is clock-neutral at the top level and claims neither clock. New counters: `sites=7427 anon=6`. Per stage it is not neutral: CORE.c `decode-total` 133.77 ms -> gone, `static-lex-values` 10.15 -> `static-lex-drain` 0.20, load road proper 263.31 -> 200.99, but `deserialize-program` 164.02 -> 271.17 and `sc-stub`/`sc-finish` 101.60 -> 176.42 (the record decode moved into the fills; the mapped slices pay page faults where v1 paid one sequential decompress), so `load-total` 653.59 -> 676.80. nqp.jar `load-total` 662.37 -> 612.75 (-7.5 %).
Task 10: docs written -- docs/jvm-unit-lazy-loading.md (new: the v2 format, the load road, lazy bodies, site identity, the dispatch table, diagnostics, what phase 2 will do); docs/jvm-perf-findings-2026-09.md "Milestone 7, Phase B" (the row, the per-stage tables, the sizes, the build and every gate clock, the twelve plan rulings plus the fifteen execution rulings, what Phase C inherits, the deferred minors collected, the uncommitted-stage0 state); docs/jvm-truffle-only-plan.md (position: Phase A and Phase B bullets, the cold-start row now "phase 1 DONE"); docs/jvm-eval-server.md (unit stores are mapped once per path and shared, nothing to forget, restart after a rebuild); the lazy-loading spec's Revision 4; the milestone-7 spec's "Phase B: closed" note. Every added line ASCII-clean (0 matches).
Task 10: docs commit = the commit carrying this line (rakudo tree; docs + this ledger + the milestone-7 spec; stamp 2026-09-15T21:30:00+02:00, single Co-Authored-By trailer). Nothing else in the rakudo tree committed; no jar exists there to commit.
Task 10: complete; PHASE B CLOSED (rakudo = the commit carrying this line, nqp 318558c2d, pushed; stage0 v2 uncommitted); NEXT = Phase C plan (C0 the spike first)
Task 10 (appended AFTER the docs commit, so these lines are UNCOMMITTED): docs commit = rakudo 1ec8bc9068 (7 files, 889 insertions, 2 deletions; stamp 2026-09-15T21:30:00+02:00 author and committer; single trailer "Co-Authored-By: Claude Fable 5.1"; no jar in the commit).
Task 10: handoff rebase, rakudo -- NOT DONE. `git fetch origin` brought 48 new commits on origin/main; `git rebase origin/main` had 211 of our commits to replay and stopped at 42/211 with a real content conflict in src/Raku/ast/signature.rakumod, our f4f4c2ff1f ("check a definite parameter type as its base type plus concreteness", which added the $definedness-done guard) against upstream's rework of the same block (nqp::isconcrete($definite) over $!type.IMPL-TARGET-TYPE.definite or self.IMPL-CAPTURE-DEFINITE). That is a frontend semantics merge, not a docs conflict, and the standing rule is "no gate after the handoff rebase" (user, 2026-09-09), so resolving it here would have shipped an unverified setting compile and possibly 169 more commits of the same. ABORTED; the tree is back at 1ec8bc9068, clean, and the measured state is intact. The rebase needs a session that can build and gate. Recorded in the memory and the report.
Task 10: handoff rebase, nqp -- SKIPPED as ruled: `git fetch upstream` then `git rev-list --count HEAD..upstream/main` = 0, so there was nothing to rebase onto. The nine ` M src/vm/jvm/stage0/*.jar` lines are unchanged and unstaged; `git stash list` is empty.
Task 10: pushed -- rakudo 36ce45c326..1ec8bc9068 -> ab5tract/worktree-jesp-direct-lazy-records; nqp fdab66706..318558c2d -> ab5tract/jesp-direct-lazy-records. The nqp tree's third remote `origin` is the STALE checkout's nqp working tree (/home/longwalker/code/raku/x.core/rakudo/nqp) and its jesp-direct-lazy-records sits at c17d93d270, i.e. Phase A's base: nothing recent was pushed there, and the standing rule is never to touch the stale checkout, so it was left alone. NOTE FOR THE RECORD: the pushed nqp branch carries the COMMITTED v1 stage0, which its own runtime can no longer read, until the user lifts the no-jar rule; the worktree is the source of truth meanwhile.
Task 10: implementer COMPLETE. Rig row b (rig wall 70 s): cold rakudo-e 2.461 s (a6 2.504), cold nqp-e 1.160 s (a6 1.192), misses 5667 (5661), hits 100711 (100697), warm t/01-sanity 50 s (a6 51 s) -- clock-neutral within the series' spread; per stage CORE.c decode-total 133.77 ms gone, static-lex 10.15 -> 0.20 ms, load road 263 -> 201 ms, but deserialize-program 164 -> 271 ms and sc 102 -> 176 ms, load-total 654 -> 677 ms; nqp.jar load-total 662 -> 613 ms; stats sites=7427 anon=6. Docs commit rakudo 1ec8bc9068 (7 files, +889/-2). Pushed: rakudo 36ce45c326..1ec8bc9068 -> ab5tract/worktree-jesp-direct-lazy-records; nqp fdab66706..318558c2d -> ab5tract/jesp-direct-lazy-records (nqp's local origin is the stale checkout, left alone). nqp upstream/main 0 ahead: no rebase.
Ruling: the rakudo handoff rebase onto origin/main was ABORTED at commit 42/211 on a semantic conflict in src/Raku/ast/signature.rakumod (our f4f4c2ff1f vs upstream's IMPL-CAPTURE-DEFINITE rework); the implementer's call stands: a conflict in a RakuAST file needs a setting compile to verify and "no gate after the rebase" does not cover a hand-merged conflict. The rebase is deferred to the next session's start (Phase C), with a make as its gate -- costs if wrong: one more day of drift from upstream (upstream brings bugfixes only).
Ruling: Task 10's task review is folded into the final whole-branch review (its diff is documentation of the branch the final reviewer reads anyway; one reviewer, both packages) -- costs if wrong: nothing (docs).
Task 10: complete (rakudo 107eca63a3..1ec8bc9068; b row taken; pushed; rebase deferred with a ruling)
FINAL whole-branch review (Opus): "ready with fixes". Strengths: O(1) shells with no record decode; the lazy-body memory model correct (compile-time barrier instead of the survey); laziness not defeated by the SC read (static-lex-drain 98 blocks vs 49 963 rows in v1); the namespace fix argued from both directions; ordinal by wire offset; honest measurement; docs true except one number. Critical 1: the branch as pushed cannot bootstrap itself -- the v2 stage0 exists only as nine uncommitted jars in this worktree (user rule) -- Ruling: not a code fix; the nine jars are now copied to /home/longwalker/code/raku/x.core/stage0-v2-2026-09-15/ (durable, outside any git tree) so one `git clean` is not fatal; the commit waits for the user to lift the rule. Important 2: the per-program slot invariant (encoder count vs builder ordinals) is only aggregate-verified; unit-check is per unit while site-check is per program -- fix wave: a per-program `unit-check-prog <namespace>#<idx> slots=N` line plus a knob-gated runtime assertion. Important 3: the docs' "anon=6 = helper/rv-decont/indy sites" is FALSE: sites/anon count only EngineSite caches (wire DISPATCH instructions); helper, rv-decont and indy DispatchCallSites are never counted; the six are the -e script's -- fix wave: correct both docs and the findings. Important 4: ruling 3 (dead call-site table) is only half done: Ops.kt:2885 and Dispatch.kt:232-259 (siteTables ClassValue, a class-road relic) still index the permanently empty table -- fix wave: delete them and getCallSites(). Minors taken into the fix wave: parsedKey keyed by name only for programs (5), non-negative index in parse (6), injective programIndex require + outerQbid lower bound (8), asReadOnlyBuffer on heap slices (9), StaticBodySource KDoc on a throwing fill (10), unit-load-exclusive.raku's decode-total (12), 123-unit-artifact.t's `5 entries` assertion (15). Left: 7 (canonical path), 11 (mappings never unmapped), 13 (runtime.bootclasspath property), 14 (legacy Perl path comment), 16.
Ruling: the reviewer's proposal to reverse the `--javaclass=perl6` deferral (give the five Rakudo units distinct ids) is deferred to Phase C's plan: it touches the Configure templates and needs a Rakudo make to verify; the namespace makes it harmless for every process-local key, and Phase C's persisted records are the first consumer of a real artifact identity -- costs if wrong: Phase C starts on five units sharing an id and must key by content or path anyway.
Ruling: the m7-rig --parse-sweep chunked-log gap is fixed before the milestone close's whole-t/ run (Phase C's plan), not now.
Final fix wave: nqp b51c1a0db, rakudo 34aeb8aa8; site-check 2598 programs, 0 violations; gates: JUnit 36/36 in 2 s, 123/125 ok, rakudo-j smoke ok, nqp suite 155 green in 193 s
Final fix wave re-review (Opus): all ten findings ADDRESSED, no new breakage; the surviving Dispatch.dispatch keeps the MethodType the bootstrap resolves; read-only slices confirmed unwritten; 123-unit-artifact.t's five entries derived from the writer. Out-of-scope minors (deferred): site-check.raku's `(\S+)` token cannot cross a space in a store path; site-check.rakutest spurts a temporary fixture into the tree (a $*TMPDIR file would be cleaner); two of its 12 assertions are near-vacuous on the clean fixture; unit-check-prog is high-volume (29809 lines for 26 units) -- knob-gated, fine as a diagnostic.
PHASE B CLOSED (2026-09-15): rakudo 34aeb8aa8 (+ this ledger commit), nqp b51c1a0db, both pushed to ab5tract; nine v2 stage0 jars UNCOMMITTED in the nqp working tree (user rule), backed up at /home/longwalker/code/raku/x.core/stage0-v2-2026-09-15/. Open for the next session, in order: (1) the deferred rakudo rebase onto origin/main (conflict in src/Raku/ast/signature.rakumod; gate with a make); (2) the Rakudo side of the fix wave (call-site residue removal) has only the rakudo-j smoke, not a make/t/01-sanity -- the same make gates it; (3) Phase C plan (C0 the spike first), inheriting: compression of stored entries (unit.records/unit.programs only; unit.serialized stays raw for phase 2), the five `perl6` unit ids, the m7-rig chunked-log parser, the corrupt-artifact bounds before slots are written, the harness low-memory guard that killed the single-server t/02 sweep four times (the whole-t/ close needs a run outside it or a smaller heap), site counting for runtime-made sites.

# SDD ledger — plan: docs/superpowers/plans/2026-09-09-jvm-unit-artifact-milestone-2.md

Spec: docs/superpowers/specs/2026-09-09-jvm-unit-artifact-design.md (reachable, binding; milestone 2 line + section 2 last paragraph + section 3 nested paragraph).
Trees: rakudo worktree (docs, gate) at 87f9956217; nested nqp/ tree (all code) at 56adfe211, branch jesp-direct-lazy-records.
User decisions (2026-09-09, brainstorm): record road stays behind NQP_UNIT in milestone 2, spec's deletions move to milestone 3; execution subagent-driven.
Baseline: NQP_UNIT=1 clean buildJvm 299 s (16:20), 12 stage2/share jars unit.meta=1 class=0.

## Pre-flight scan (2026-09-09)

| pair / task | produces vs consumes | finding |
|---|---|---|
| T1 -> T2 | syscall `jvm-build-unit` (OBJ, OBJ) -> EvalResult{record}; Backend.nqp `nqp::syscall('jvm-build-unit', $jast, %jastnodes)` | consistent |
| T1 -> T2 | loadcompunit retention key = rec.meta.unitId = JastClass.className = $*JCLASS.name; jvmclassofcuid returns it; Compiler.nqp puts it in nested_classes; UnitWriter.record looks it up in inMemoryUnitRecords | consistent (same string on both roads) |
| T1 -> T3 | stderr marker `unit record ` (trailing space, line start) in loadcompunit vs test's nqp::index(..) == 0 | consistent |
| T2 -> T3 | knob die text contains `needs NQP_CODE_RUN=1`; test greps that substring | consistent |
| T2 -> T4 | Task 2 build is the gate build; Task 3 adds a test only (no rebuild, forward only) | consistent |
| T1 internal | test asserts staticInfo.uniqueId from BlockRec.cuid; CodeRef ctor takes uniqueId (CodeRef.kt:52); lookupCodeRef(String) is `open` (CompilationUnit.kt:265) | consistent |
| T1 internal | Step 9 smoke = old stage2 compiler (milestone-1 road rule) + new runtime jar: class-road compilejast/loadcompunit branch unchanged | consistent |
| T2 internal | knob check reads %*COMPILING<%?OPTIONS><target>; a nested compile (World.create_code) may lack the key -> null ne 'classfile', no die | consistent (same read as milestone 1's rule) |
| T2 internal | wrapper condition adds ($*UNIT_ROAD && %*BLOCK_LEX_VALUES): hash state at that line is what the class road's setup_blv push read | consistent |
| rubric | T3 asserts an absence ('nothing on stderr mentions a class') beside the positive marker | acceptable; reviewer may flag as weak, ruling deferred to review |
| rubric | T1 rewrites UnitWriter.write as record()+write(): a split, not a duplicated logic block | acceptable |

## Tasks
Ruling (user, 2026-09-09): Task 4 drops the knob-off t/nqp sweep -- the Rakudo make + t/01-sanity gate already covers the class road; the knob-on sweep elapsed vs 487 s is the compile-cost reading -- costs nothing if wrong beyond a later 8-minute sweep.
Task 1: implemented (nqp 6d0af267d; 15/15 Kotlin tests, smoke 42 + 123 8/8), review dispatched (sonnet).
Task 1: review Approved (sonnet). Important: UnitWriter.record resolves nested ids only in inMemoryUnitRecords, so a class-road nested unit under a record parent hard-refuses. Ruling: by design -- the knob decides the road for the whole process (plan deviation 1), so the mix cannot occur; when it would (a road bug), the hard error naming unit and nested id is what spec section 5 asks for, never a fallback -- costs a confusing error message if a mixed case ever appears. Minors (deferred): integration paths (jvm-build-unit, loadcompunit record branch, nested embedding) unexercised until Task 2/3; byCuid silently overwrites a duplicate cuid. Trailer fixed by controller amend (Sonnet -> Fable): nqp 6d0af267d -> cfa0846ac.
Task 1: complete (nqp commits 56adfe211..cfa0846ac, review clean with ruling)
Task 2: dispatched (nqp base cfa0846ac; opus)
Task 2: implemented (nqp 0b149eaeb; clean NQP_UNIT build EXIT=0 elapsed=296s, 11/11 share-lib jars meta=1 class=0; smoke: unit record + 42, EVAL 15, knob die, 123 8/8, class road 42), DONE_WITH_CONCERNS: (1) brief smoke-2 quoting bug -> plan text fix by controller; (2) 12 jars is a brief miscount, share/lib holds 11; (3) blockvalues rows for non-comp-mode units need an SC -> controller smoke: class Foo on the record road prints unit record (8 programs) + 42 (NQP install_lexical_symbol values go through add_object/setobjsc); Task 3 script gains a class for coverage. Review dispatched (opus).
Task 2: review Approved (opus), no Critical/Important. Minors (deferred): (1) record-road mainline frame loses its filename in backtraces (in <mainline> without (-e); Task 1 loading path; milestone-3 item beside the spec backtrace note); (2) Compiler.nqp calls nqp::getenvhash() twice at the road decision; (3) $*UNIT_FALLBACKS is the constant 0 (scaffolding, delete with milestone 3 deletions); (4) JASTNodes.nqp:65 comment over-long; (5) --target=jar without --output dies in HLL::Compiler result dumping (lateinit st) on EITHER road, pre-existing.
Ruling: Task 3 drops the --target=jar-without-output test case (plan(11)); the routing is verified by review and nothing observable from a child process distinguishes the roads there -- costs a gap if the dumper is fixed later and the road turns out wrong (the census line would show it).
Task 2: complete (nqp commits cfa0846ac..0b149eaeb, review clean)
Task 3: dispatched (nqp base 0b149eaeb; sonnet)
Task 3: NEEDS_CONTEXT -- NQP_CODE_WHY also prints the encoder per-block trace on stdout, so the single marker+output child run put the script lines off their expected positions (7/11 failed; marker/knob subtests passed). Ruling: two child runs -- a plain one for the seven output lines, a NQP_CODE_WHY one whose stderr carries the unit record marker; no content scanning -- costs one extra JVM start per test run if wrong. Implementer resumed.
Task 3: implemented (nqp 3e78a37a9 -> 02f6fd00e after controller subject amend; 11/11 both ways; two child runs per ruling), review dispatched (sonnet).
Task 4: dispatched in parallel with the Task 3 review (opus; sweep on the committed test file; only 124 reruns if the review changes it).
Task 3: review Approved (sonnet); trailer verified by controller (Fable). Minor (deferred): the inline comment at the marker run restates the header rationale.
Task 3: complete (nqp commits 0b149eaeb..02f6fd00e, review clean)
Task 4: gate agent (opus) stuck on a bad monitor after Step 1 (user report, 2026-09-09) -- stopped by controller; Step 1 evidence kept (step1-jars.txt: 11/11 meta=1 class=0). Ruling: controller runs Steps 2-4 itself as plain background watched-run jobs (no subagent) -- costs controller context, buys a gate that actually runs.
Task 4 Step 2: t/nqp on the record road (NQP_UNIT=1, 3 jobs): 113/118 in 381 s (baseline 487 s class-road test compiles); 019-file-ops 112/112 and 063-slurp 1/1 from the nqp dir (cwd-relative, as milestone 1) => 115/118. The three FAILs are encoder refusals, hard errors under the knob (no bytecode fallback on the record road): 059-nqpop.t <mainline> "withy general", 067-container.t <anon 24> "withy general", 084-loop-labels.t <mainline> "labeled control". Ruling: not milestone-2 regressions -- pre-existing item-7 shapes that the class road hid behind per-block fallback; recorded for the strict campaign (runtime-compile shapes: withy general, labeled control); the gate counts 115/118 with those three named -- costs nothing if wrong beyond the campaign picking them up later.
Task 4 Step 3: Configure EXIT=0 4 s (no nqp rebuild; artifact jars intact). make EXIT=0 elapsed=1274s (milestone 1: 1173 s); stage markers: rakudo.jar 195 s, BOOTSTRAP v6c 229->657 s (428 s; m1 339 s), CORE.c 657->1172 s (515 s; m1 509 s), v6d 1172, CORE.d 1175, v6e 1204, CORE.e 1207. User (2026-09-09): no compile-time investigation now; timings recorded as measured. t/01-sanity launched (2 jobs).
Task 4 Step 3-4: t/01-sanity 25/25 in 214 s; probe 1 (Rakudo -e BEGIN/EVAL) all three units records, output 1 2 3; probe 2 (module with BEGIN closure) artifact with 0 nested (closure re-pointed, no nested unit produced; deviation 2 stands). Report: task-4-report.md.
Task 4: complete (no code; gate GREEN: sweep 115/118 with 3 named encoder refusals, make EXIT=0, sanity 25/25)
Task 5: started (controller; docs + final review)
FINAL REVIEW (fable): Ready to merge = Yes; no Critical/Important defects. Important-as-notes: (1) make 1274 s vs 1173 s knob-off (BOOTSTRAP +26%) -- user ruled no investigation; milestone 3 must not treat 1274 s as a hard baseline without one more knob-off make; (2) deviations 1 and 2 confirmed correct; nested embedding exercised only by the Kotlin round trip until Rakudo flips -> first probe of milestone 3. Minors: (1) ProgramUnit.claimNested in-memory fallback is dead for the record-parent road (record.nested is filled before the parent exists) and would mask a missing nested/ entry on a disk artifact; (2) t/nqp/124 inherits NQP_CODE_RUN/PRECOMP from the harness env; (3) absence check in 124; (4) record() maps hll "" to "nqp" where the class road keeps "" -> sweep t/qast under the knob before milestone 3; (5) marker prints go to System.err not tc.gc.err (eval server; parity with the writer); (6) double getenvhash. Deferred-minor triage: all leave; backtrace filename = mandatory first item of milestone 3 after the flip probe; --target=jar dumper death -> milestone-3 census.
Ruling: fix wave = Minors 1 and 2 (one dispatch, no stage build: runtime jar + test file); Minor 1 resolved by DROPPING the fallback (spec section 5: never a fallback; the plan text asking for it was wrong) -- costs nothing if wrong beyond re-adding two lines; Minors 3-6 and the two measurements (knob-off make, t/qast sweep) are booked for milestone 3, not done here (user: no compile-time investigation now).
Final fix wave: nqp da1f5088a (claimNested fallback dropped; 124 sets NQP_CODE_RUN/PRECOMP for its children). Kotlin 15/15; 124 1..11 with switches, with switches stripped from the harness env, 123 1..8. Scoped re-review dispatched (sonnet).
Final fix wave re-review (sonnet): 2/2 addressed, no new breakage. MILESTONE 2 COMPLETE: nqp da1f5088a, rakudo docs 5dc6517b81 (+ this ledger/reports commit), both pushed.

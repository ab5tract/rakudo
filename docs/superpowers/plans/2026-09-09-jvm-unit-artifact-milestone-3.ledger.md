# SDD ledger — plan: docs/superpowers/plans/2026-09-09-jvm-unit-artifact-milestone-3.md

Spec: docs/superpowers/specs/2026-09-09-jvm-unit-artifact-design.md (reachable, binding; milestone 3 line + sections 2, 4, 6; milestone-2 line's deletions as narrowed by the plan's deviation 1).
Trees: rakudo worktree (docs, runners, gate) at 5ae25a8d3c; nested nqp/ tree (compiler + runtime) at da1f5088a, branch jesp-direct-lazy-records.
User decisions (2026-09-09, brainstorm): six-part shape as proposed; NO t/spec ("until it doesn't take hours to run t/"); two t/ sweeps (after the flip, after the deletions) each under --max=7200; no knob-off make (forward only); anonymous-block naming deferred; RakuAST `#?if jvm` edits allowed (none needed: LibraryLoader already special-cases ModuleLoader.class).
Baseline (milestone-2 gate): NQP_UNIT=1 clean buildJvm 296 s; t/nqp record road 115/118 in 381 s at 3 jobs; make 1274 s; t/01-sanity 25/25 in 214 s at 2 jobs; t/ (2026-09-05, one warm server, class road): 01-sanity 1m04 / 02-rakudo 46m33 (20 fail) / 03-jvm 0m17 / 04-nativecall 5m46 (2) / 05-messages 8m59 (4) / 06-telemetry 2m25 / 07-pod-to-text 0m20 / 08-performance 10m06 (2) / 10-qast 0m11.

## Pre-flight scan (2026-09-09)

| pair / task | produces vs consumes | finding |
|---|---|---|
| T1 encoder -> T1 builder | wire FORLOOPL=35 layout `condType lastId nrId outerIdx labelLocal labelExpr cond pre body`; encoder emits op, ct_at, lid, nrid, outer, lbl_local, label expr, cond, pre, call | consistent |
| T1 exit handlers -> runtime | frame_op=1 -> wire word 3 framed -> NqpDispatch builds a CallFrame -> cf.leave() runs hll.exitHandler over Ops.result_o(caller); StoreRet stored the value inside the call | consistent (explorer verified every road: direct, enterEngine, ProgramEntry, resume) |
| T1 labeled control -> runtime | newexception/setpayload/setextype via W_CLASSLIB (registered in Compiler.nqp:1860-1865, no :cont), throw via wire 181 answering result_o(cf); loop unwind arms mask-match NEXT\|LABELED against NEXT rows; _is_same_label compares payload hashCode | consistent |
| T2 knob default -> T2 test | Compiler.nqp die text contains `needs the encoder on`; t/nqp/124 greps that substring under NQP_CODE_RUN=0 | consistent |
| T2 runners -> UnitMain | `UnitMain <unit jar> args...` (UnitMain.kt:12-19); create-jvm-runner.pl's $postamble appends `"$@"` after $command, so `... UnitMain <app> "$@"` | consistent |
| T2 precomp -> loader | CompUnit::Loader.rakumod:46-48 `nqp::loadbytecodebuffer` -> LibraryLoader.load(tc, ByteBuffer) sniffs UnitZip.isUnit(buffer) -> UnitLoader.loadAndRun(bytes) | consistent (no writer change: Backend.nqp keys on target+output, not extension) |
| T2 dumper -> backends | `$!backend.is_compunit` exists on jvm (Backend.nqp:117), moar (:828), js (:358) | consistent |
| T4 deletions -> stage0 | stage0 jars run on this runtime and are class-road units with codeprograms.lz4 sidecars; javap shows only codeRunIdx call sites | runtime writer/loader stays (deviation 1); codeRun(String) private |
| T4 JASTNodes -> JastClass.kt | attributes deleted on both sides in the same task; UnitWriter reads programs/callsites/blockvalues/nestedClasses/className/hll + cr_* only | consistent |

## Rulings, deviations, deferred minors (filled as tasks run)

- Known gap, not worked: torn-frame LEAVE never runs on the JVM (CallFrame.countLeft / giveBackTornFrames), both roads, pre-existing; S04-phasers will show it when t/spec runs.
- Known gap, not worked: interop adaptors (RakudoJavaInterop.kt:930-937, BootJavaInterop.kt:204-209) subclass a generated CompilationUnit through ByteClassLoader (spec item 9).
- Deferred (user): anonymous block names in backtraces.

## Task log
Task 1: dispatched (implementer opus; BASE rakudo 40145235cb, nqp da1f5088a; brief task-1-brief.md; report task-1-report.md)
Task 1: controller note sent — the Rakudo make was a no-op (EXIT=0 in 0 s: Rakudo rules depend on the nqp version file, not the stage jars); told the implementer to Configure.pl --gen-nqp then make before sanity/probes. Ruling: a 0-second make never counts as a gate — costs one ~25 min rebuild if the implementer had already handled it.
Task 1: implementer DONE_WITH_CONCERNS (nqp c872c83af): build 287 s strict, 11 jars meta-only; t/nqp 118/118 record road; Rakudo make 1233 s after `make clean` (implementer chose j-clean over Configure --gen-nqp to keep the unit-meta nqp jars); sanity 25/25; 9 exit-handler probes + withy shapes green; concerns = make no-op lesson, two probes rewritten (nqp objects lack .defined; `will leave` placement), FORLOOPL constant placed beside FORLOOP. Reviewer dispatched (opus) on task-1-review-package.md.
Task 1: review Approved (opus), spec compliant, no Critical/Important. Controller closed the ⚠️ (value/2-child withy branch unchanged) with a record-road probe: with(D.new,"t") -> t; with(undef-obj,"t") -> the condition; without(undef-obj,"w") -> w. Minors (deferred): (1) that probe was absent from the report [now closed]; (2) `%e<hidx> := $outer` no-op inside the for :label block; (3) FORLOOPL builder case duplicates FORLOOP ~30 lines (plan-mandated by wire additivity) -- add keep-in-sync comments; (4) FORLOOPL constants out of numeric order in NqpWire.java and TruffleEncoder.nqp; (5) `if $label` vs nqp::defined at sibling sites; (6) frameFreeEntryOk exit-handler guard sits before the sameHll fast path (hot path). Ruling: minors 4 and 6 are folded into Task 2 (it touches both files anyway); 2, 3, 5 wait for the final review.
Task 1: complete (nqp da1f5088a..c872c83af, review clean)
Task 2: dispatched (implementer opus; BASE rakudo 40145235cb, nqp c872c83af; brief task-2-brief.md; report task-2-report.md; carried: make no-op lesson, gradlew --stop before the defaults build, fold-in of Task 1 minor 4 (constant order) only, hll ""/"nqp" watch in t/qast)
Task 2: Claude Code process restarted mid-Step 10; the implementer was auto-restarted from its transcript; its make died at CORE.c jast (t2-make.log truncated, no java alive); controller told it to rerun make (resumes at CORE.c) and re-arm its watch. rakudo edits uncommitted on disk at that point (create-jvm-runner.pl, rakudo-j-build.in, docs/jvm-eval-server.md); nqp t2-build EXIT=0 236 s under bare env (defaults proven).
Controller (user request, visibility): rakudo 5641d0232f adds watched-run.raku --follow=LOG (attach to a log another watched-run writes; streams its mirrored [Ns] markers + own --show until the EXIT line). Tooling commit outside the plan, on the rakudo tree between Task 2 BASE and its HEAD; the Task 2 reviewer is told. gate-watch relay now also echoes the newest [Ns] marker per log every 90 s.
Task 2: user note -- the box entered sleep mode during this task (likely the cause of the session restart); Task 2 timings (make 566 s resumed from CORE.c, the earlier partial make, any sweep) are NOT a baseline. Ruling: the next full make (Task 4, after Configure) is the milestone-3 timing baseline; sleep suppressed from here on.
Task 2: implementer iterated after the first make -- t2-build3 (nqp, 232 s, EXIT=0) and t2-make2 (full make from the top, 1306 s: rakudo.jar..BOOTSTRAP ~600 s, CORE.c 609 s, CORE.d/e by 1306 s; EXIT=0) at 22:15-22:37, sleep suppressed by then; a fix wave whose reason the report will name. make2 numbers are candidate baseline figures if the report confirms sleep was already off.
Task 2: implementer DONE_WITH_CONCERNS (nqp 388173781, rakudo 08a997dc2b): defaults build no knobs, 11 jars meta-only; t/nqp 118/118 + t/qast 02 ok (01-qast.t fails at test 10 + dies on missing HLL::Backend::JVM.start, identical on the class road -> pre-existing, deferred); Configure + full make (16 Rakudo jars meta-only, CORE.c nested=8); sanity 25/25; t/03-jvm + t/10-qast 2/2; precomp 14/14; BEGIN/EVAL 3 records; backtrace + dumper probes correct. Two flip regressions fixed in TruffleEncoder.nqp (BVal naming a block of the same tree; with/without over a native condition, which broke every `use Test`). Concerns: timings sleep-inflated (CORE.c 609 vs 474 s) -> not a baseline (already ruled); commit Co-Authored-By names Opus not Fable; added tools/build/jar-census.raku (Raku). Reviewer dispatched (opus) on both packages.
Ruling: the Opus Co-Authored-By line on the Task 2 commits stands (the committing agent WAS Opus; the Claude-Session trailer is present) -- costs nothing if wrong beyond a history rewrite the user can do.
Task 2: review Needs fixes (opus): Important 1 = block_in_tree descends into nested blocks -> a BVal into a deeper block can compile under the wrong outer (was a loud cbail); Minors: (2) emit_defined_test magic 0 for T_OBJ; (3) redundant `line < 0` term in mappedLine (brief-verbatim); (4) unquoted $jardir in the runner exec line (file precedent); (5) jar-census.raku word-splits paths with spaces and counts any entry containing nested/ (fine for this writer); (6) attribution (ruled: stands); (7) nqp/t/qast gate 1/2 -- 01-qast.t is moar-only API (verified statically), pre-existing; (8) timings not a baseline (ruled). Ruling: fix round 1 = Important 1 + Minor 2, NO stage build (validated by Task 4 build; forward-only rule); Task 3 sweeps on the current jars. Costs: if the narrower walk refuses a CORE.c shape, Task 4 make dies with `bval to an uncompiled block` and one more line fixes it.
Task 2: fix round 1/5 (2 addressed by implementer claim, unvalidated by build per ruling -- block_in_tree stops at nested blocks with a $root flag for the call site; emit_defined_test default $T_OBJ; refusal renamed `bval to a block the unit never compiles`; nqp 388173781..e5f2b3840). Scoped re-review dispatched (sonnet) on task-2-fix1-package-nqp.md.
Task 2: fix round 1/5 (2 addressed, 0 open; re-review sonnet: no new breakage; nqp 388173781..e5f2b3840). Carry to Task 4: grep its build/make logs for `bval to a block the unit never compiles` / `has no engine program` (the narrower walk unvalidated until then). Minors (deferred): mappedLine redundant term (brief-verbatim); unquoted $jardir in the runner exec (file precedent); jar-census.raku path quoting + nested/ counting note; t/qast/01-qast.t moar-only (pre-existing); Task 1 minors 2,3,5 still open.
Task 2: complete (nqp c872c83af..e5f2b3840, rakudo 40145235cb..08a997dc2b incl. controller tooling 5641d0232f; review clean after 1 fix round)
Task 3: dispatched (sweep, sonnet; jars = Task 2 make2 build (nqp 388173781 + rakudo 08a997dc2b); --jobs=3 per CLAUDE.md; --max=7200; report-only, no edits; log t3-sweep.log)
Task 3: user decision 2026-09-10 -- if the 7200 s ceiling fires, the unreached directories run in a second invocation under their own --max=7200 (log t3-sweep2.log, markers t3-sweep2.markers); the report carries both wall clocks. Sent to the sweep agent.
Task 3: sweep 1 hit the ceiling -- EXIT=124 timeout at 7200 s after chunk 35/38 (2 servers x 6g: the 21g budget fit two, not three); ~11 no-TAP chunks; 33 files unreached. Second invocation for the unreached directories under its own --max=7200 per the user (t3-sweep2). The 7200 s itself is the finding the user wanted before t/spec: t/ on artifact units takes over two hours on this box with two servers.
Task 3: DONE_WITH_CONCERNS (sweep agent, sonnet): sweep 1 EXIT=124 at 7200 s (35/38 chunks, 2 servers), sweep 2 658 s (5 chunks); targeted rerun of 90 files from failed chunks 815 s (sweep 1 timed out before printing per-file detail). Wall clock for t/ on artifact units: 7858 s over two runs, vs 78 min on one class-road server on 2026-09-05. NEW failures: (a) 6 encoder refusals -- sized typed param x3, sized uint attributeref x1, unnamed chain link x2; (c) int8 preinc wrap (128 for -128), EVAL-in-BEGIN NPE cluster x3, generated-populate.t CCE Long->SixModelObject, xx-sink-lazy.t continuation at non-suspendable engine site, yada-trait-timing.t trait arity SORRY, corekeys/settingkeys x7 (NFC/NFD/NFKC/NFKD/Uni now in CORE::v6c -- NFG work, test expectation, NOT unit-road); (d) 3 chunks (33 files) pass on rerun (server heap growth). FIXED since 09-05: 6 files (21-begin-time-compile-sub, dd, doc-block-statement-owner, has-list-declaration, n-flag-mainline-scope, repl). Concern: --jobs=3 was in the dispatch prose but not the brief command line; auto-sizing gave 2 servers.
Ruling: insert Task 3b "unit-road gaps from the t/ sweep" before Task 4 -- the 6 refusals + the 5 engine-correctness causes are the unit road losing its fallback and must be in Task 4 build so Task 5 measures the deletions; brief written by the controller (task-3b-brief.md, appended to the plan); no Rakudo make in 3b (test-file blocks are runtime compiles; CORE re-encodes in Task 4). Deferred to ledger: corekeys/settingkeys NFG expectation (Rakudo test lists); sweep heap flakes; --jobs=3 to be on the command line in Task 5. Costs if wrong: one extra nqp build + ~1 h of investigation before the deletions.
Task 3: complete (report task-3-report.md; no code)
Task 3b: dispatched (implementer opus; BASE nqp e5f2b3840; brief task-3b-brief.md; report task-3b-report.md; no Rakudo make; one stage build; markers files t3b-build/t3b-sweep/t3b-sanity)
Task 3b: implementer DONE_WITH_CONCERNS (nqp e5f2b3840..171d37508, 6 commits): items 1-7 fixed (+ begin-native-var as a uint lexicalref refusal); item 8 (yada-trait-timing arity SORRY) analysed, unfixed -- suspected NqpDispatch capture assembly. Gates: build EXIT=0 0 bails 11 jars; 11/13 files cold; wrap probe -128/0/-32768; t/nqp 118/118 + t/qast 1/2; sanity 25/25. Concerns: encoder changes invalidate rakudo.jar via the QAST.jar dependency so it relinked Rakudo twice (CORE jars no longer the flip build; Task 4 makes from the top anyway); item 8 leaves 2 files red; the "modified at session start" rakudo files were Task 2 edits since committed (nothing lost). Reviewer dispatched (opus).
Task 3b: Ruling on item 8 -- minimal reproducer `BEGIN { sub f(Int $x where * > 2) { $x }; say f(5) }` (a where-constrained param of a routine declared AND called in one BEGIN): the WhateverCode $!do is bound to the dyn unit mainline code ref and called with a null. A unit-road regression in code-ref pairing for a dynamically compiled unit (jvm-repoint-dynamic-code by cuid / IMPL-FIXUP-COMPILED-CODEREFS via getcodecuid+compunitcodes / ProgramUnit.byCuid), so it stays in milestone 3: one time-boxed fix round (same implementer, ~90 min) after the review; not fixed -> parked, 2 files red in sweep 2, documented. Costs if wrong: 90 min.
Task 3b: review Needs fixes (opus). Important: (1) fifth BlockInfo walk unguarded at TruffleEncoder.nqp:3013; (2) IsTypeOp reaches user code (decont Proxy, accepts_type where-block) but has no SaveStackException arm; (3) resolve_lexref runs twice per lexical bind (hot path; CORE.c parse regression tracked); (4) item 8 routing -- (a)/(b) identity fault over (c). Minors: traceArityRefusal NPE-unsafe + fires on SaveStackException; NQP_CONT_TRACE per-call getenv; double truncation on lexical sized params (idempotent); num32 store on the run boundary; sized_ret falls back to $var.returns; nested unnamed chain evaluates callee after operands; op 382 documented in NqpOps not NqpWire; NQP_VERBOSE_EXCEPTIONS output grew; nqp-runtime:test evidence missing. Fix round 1 dispatched to the same implementer: Important 1-3 + item 8 (90 min, (a) then (b)) + the traceArityRefusal minor + the test evidence; one stage build + cold reruns.
Task 3b: fix round 1/5 (3 addressed + minor; item 8 open; nqp 171d37508..620d4c181: fifth walk guarded, resolve_lexref once, typed suspend tokens for classlib + istype/isconcrete/hllize arms, NQP_REPOINT_TRACE). New lead on item 8: ProgramUnit.buildTable skips null qbid slots while the serialized code-ref fixup is positional by qbid (spec: the table tolerates gaps) -> code refs bound off by the gap. New hole: NqpOps.run hardcodes T_OBJ for generic table-road suspend tokens. Fix round 2 dispatched (same implementer): codeRefs positional by qbid + table-road rtype; re-review of rounds 1+2 together after.
Task 3b: fix round 2/5 (table-road rtype FIXED via additive wire tag OPCALLT=36; item 8 NOT fixed -- the qbid-gap lead was wrong: codeRefs is dense on both roads, qbidToCodeRef is the positional array and is correct; real tie = Code.clone of a $!do already bound to the wrapper code ref, next lead the getstaticcode stub at code.rakumod:428; nqp 620d4c181..8bab02391). Sanity 25/25 on the OPCALLT jars. New concern: a suspended typed op resumes with the inner call value, not by re-running the op (istrue of a take-ing Proxy answers 7 not 1) -- type-correct, value-incorrect; narrow shape.
Ruling: item 8 PARKED after two time-boxed rounds -- real, narrow (a where-constrained param of a routine declared+called in one BEGIN; trait application timing), two t/02-rakudo files red in sweep 2; evidence and next lead in task-3b-report.md; nothing downstream in this milestone builds on it. Ruling: the resume-value concern is parked too (pre-existing road semantics; class road re-enters at the save site) -- a follow-up item, ledgered for the final review. Costs if wrong: the two files stay red into milestone 4; a take inside a Proxy FETCH inside a typed op answers the wrong value.
Task 3b: scoped re-review of rounds 1+2 dispatched (opus) on task-3b-fix12-package-nqp.md (nqp 171d37508..8bab02391).
Task 3b: re-review (opus) all findings addressed, no new breakage; OPCALLT verified additive (tag 36, OPCALL 15 untouched, emitted only for non-object table ops, read word for word; stage0 programs unaffected); out-of-scope: IsTypeOp arm currently unreachable (istype travels as CLASSLIB; the typed classlib token fixed the probe), resume-value gap ledgered, two widened ops lose long boxing-elimination (cached boxes).
Task 3b: complete (nqp e5f2b3840..8bab02391, review clean after 2 fix rounds; item 8 PARKED with ruling above)
Task 4: dispatched (implementer opus; BASE nqp 8bab02391, rakudo 99d7b97d5d; brief task-4-brief.md; carried: --show-file on every watched-run, clear lib/.precomp + t/packages/Test-Helpers/.precomp after the make, jar-census.raku for the census, full make = timing baseline, item-8 pair not in the gate, git status submodule quirk)
Task 4: implementer DONE (nqp 30e849e3c, rakudo a22eb40b73): build 222 s, 11 jars; t/nqp 118/118, t/qast 1/2; Configure 3 s, Makefile no longer exports NQP_CODE_RUN; make 1154 s FROM THE TOP = milestone-3 timing baseline (rakudo.jar 171, v6c 200, CORE.c 594->1069 = 475 s, CORE.d 1069, CORE.e 1096); 16/16 Rakudo jars artifacts, CORE.c nested/=8; sanity 25/25 (161 s); precomp 14/14 (one needs RAKUDOLIB=lib in the harness, Task 2 artifact); t/03-jvm + t/10-qast 2/2; zero refusal hits; leftover grep empty. Concerns: stage1 jars carry NO sidecars (brief wrong; stage0 does); 4 stale NQP_UNIT comments in runtime .kt deferred (a .kt touch costs a jar rebuild); t/qast/01 moar-only. Reviewer dispatched (opus).
Task 4: review Approved (opus), spec compliant, no Critical/Important. Minors (deferred to Task 6 / M4): CLAUDE.md:18 still says the Makefile exports NQP_CODE_RUN/PRECOMP (Task 6 docs); 4 NQP_UNIT comments in runtime .kt (M4); t/nqp/123+124 headers and env still name NQP_UNIT=1 (inert; Task 6 fix wave if cheap); setup_blv core op + ProgramUnit reference dead (M4); Backend.nqp:98 jar() comment names the removed path; Makefile.in:11 comment formatting.
Task 4: complete (nqp 8bab02391..30e849e3c, rakudo 99d7b97d5d..a22eb40b73, review clean)
Task 5: dispatched (sweep 2, sonnet; jars = Task 4 build; --jobs=3 --heap=4 on the command line this time; --max=7200; second invocation for unreached dirs under its own cap per the user; expected red: yada-trait-timing.t + begin-time-attributive-param-method.t (parked item 8), the 7 corekeys/settingkeys files (NFG expectation), the 2026-09-05 known list minus the 6 fixed)
Task 5: DONE_WITH_CONCERNS (sweep agent, sonnet): sweep 2 = 7200 s ceiling after 59/60 chunks (3 x 4g, 7-file chunks) + 70 s tail = 7270 s; ~413 files inside the ceiling vs ~385 in sweep 1 (11-file chunks at 6g). NO new failures since sweep 1 (verified by a 42-file targeted sweep + 10 cold reruns; main-sweep chunk attribution unreliable). FIXED since sweep 1: 26 (11 expected + 15 presumed-failing that pass). Per dir now / 2026-09-05 record: 01 0/0, 02-rakudo 13/20, 03 0/0, 04-nativecall 0/2, 05-messages 1/4, 06 0/0, 07 0/0, 08-performance 2/2, 10 0/0, 13 0/-, 14 0/-. Expected red confirmed: item-8 pair, corekeys/settingkeys cluster. Concern: ~10 FAIL chunks beyond the content-predicted 11 attributed to heap growth across long chunk sequences (sweep infrastructure). A 77-file supplementary sweep left running in the background (non-blocking).
Ruling: report-only tasks (3, 5) get no code review (no diff); their reports are the artifact.
Task 5: complete (report task-5-report.md; no code)
Task 6: docs dispatched (opus): plan position rows, spec milestone-3 line, AGENTS.md knob sentence, the docs twin of this ledger, reports kept; memory by the controller; final whole-branch review after the docs commit; then rebase + push.

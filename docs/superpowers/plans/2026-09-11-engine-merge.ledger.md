# Engine merge — SDD ledger (record; the SDD workspace is deleted after the branch closes)
# SDD ledger — plan: docs/superpowers/plans/2026-09-11-engine-merge.md

Spec: approved in chat 2026-09-11 (restated in the plan's Spec paragraph); no separate spec file — rulings are against that paragraph.
BASE before Task 1: rakudo e0e4bbc35d, nqp df564ddbb (nqp working tree already carries: NqpPolyglot.kt, RxMatchRootNode.kt new; TruffleGrammarEngine.kt -> NqpGrammarEngine.kt renamed+edited; RxLanguage.java deleted — written by the controller before the plan existed; Task 1's reviewer sees them in the diff).
Model policy (user rule 2026-09-11): subagents on Opus; Fable only after erroneous output.

## Pre-flight scan

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 -> T2 | T1: jars via syncRuntimeJars, nqp commit; T2: runs ./rakudo-j and nqp-j-gradle against them | consistent (runtime-only; T2 kills stale eval servers first) |
| T1 -> T3 | T1: class `NqpGrammarEngine`, id `nqp`; T3: docs name `NqpGrammarEngine`, `NqpPolyglot.kt`, id `nqp` | consistent |
| T2 -> T3 | T2: verdict line; T3: quotes it in the commit message | consistent |
| T1 internal | Step 9 grep expects hits only in the historical javadoc sentences; Step 11 expects NqpCheck's unchanged literal `nqp-code check passed` (NqpCheck is not edited) | consistent |
| T1 internal | Step 13 `git add -A nqp-truffle/src` covers the two untracked Kotlin files and the rename/delete | consistent |
| T3 internal | line numbers (194, 97, 12, 10, ~84) are hints; the sed/anchor text is the instruction | consistent |
| Rubric | no test that asserts nothing; no duplicated logic block (Matcher moves verbatim, its only copy) | clean |

Ruling: review packages for nqp-tree tasks are built by hand (`git -C nqp log/diff -U10 BASE HEAD > <workspace>/task-N-review.diff`) — the review-package script runs against the rakudo tree, and the nested nqp tree is a separate repository — costs nothing if wrong beyond a reviewer reading a hand-built file of the same shape.

## Task loop

Task 1: implementer dispatched (opus) at 23:31; BASE nqp df564ddbb.
Task 1: implementer DONE_WITH_CONCERNS — nqp b34bc52e5; build ok, smoke ok, regex harnesses pass by hand through the merged language; the three gradle harness tasks fail identically at the parent df564ddbb (harnessClasspath lacks nqp-runtime since nqp 18a1a7835; nqpcheck's canned `serial` round trip dies on a non-Long constant). Report: task-1-report.md.
Ruling: the harness gate (brief Step 11) is unmet for pre-existing reasons; fix it in this task as a SEPARATE nqp commit (stamp 2026-09-12 22:58:00 +0200) rather than park it — a red harness is not a gate. (a) harnessClasspath gains nqp-runtime and its runtime deps, Truffle jars kept OFF the classpath (module path only). (b) the canned `serial` program and `roundTrip` are DELETED, not repaired: Bytecode-DSL serialization is used nowhere in production (grep: only NqpLanguage.roundTrip calls serialize/deserialize; precompiled units ship wire strings), so the check verified dead machinery and its "precompilation in miniature" comment was false. Costs if wrong: someone wanting DSL serialization later re-adds a round trip (the deleted one was broken anyway).
Ruling: task reviewer (opus) dispatched on df564ddbb..b34bc52e5 in parallel with fix round 1 (harness fix) — the reviewer reads a static package and is read-only; the fix round's commit is covered by the scoped re-review. Costs if wrong: a reviewer's focused working-tree check sees the fix's edits early; harmless.
Task 1: deferred minor — `parse` no longer rejects an unknown (non-nqpp, non-code-test, non-rxd) source with a wire-form message; a truncated program string would surface as a regex parse error (implementer concern 2; design-specified fall-through).
Task 1: review (opus) — Spec ✅, Approved, 0 Critical/0 Important, 5 Minor. Named-risk checks passed: CodeEngine.compile call sites, IMPL strings, no stale ids anywhere, @Child on the Kotlin field (javap), stateOut!! behaviour-preserving, lazy Context SYNCHRONIZED. ⚠️ Step 11 gate unmet (pre-existing) -> fix round 1 already dispatched.
Task 1: minor (deferred): NqpLanguage.parse fall-through hands a head-corrupted nqpp string to the regex parser (guard on `startsWith("nqp")` would restore the old diagnostic).
Task 1: minor (deferred): NqpPolyglot.compile failure message no longer says which road was expected (branch on NqpWire.isProgram).
Task 1: minor (deferred): ConstantRootNode public in RxMatchRootNode.kt though it serves the program road too (`internal` would keep the old visibility).
Task 1: minor (deferred): RxMatchRootNode.compileSource public @JvmStatic where the original was package-private (`internal`).
Task 1: minor (deferred): code-side Source names (cf.codeRef?.name / sci.methodName) unsanitized vs the grammar side's NUL guard; cosmetic.
Task 1: note: nqp-runtime.jar and nqp-truffle.jar are now a matched pair (compile arity); a mismatch surfaces as AbstractMethodError at first block run; eval servers must be restarted (carried into Task 2).
Ruling: the worktree's CLAUDE.md (newer than the main checkout's, loaded at session start) says NQP_CODE_RUN/NQP_CODE_PRECOMP must NOT be set; the plan's Global Constraints and the Task 1/2 briefs said to set them to 1. Plan + briefs corrected to RAKUDO_RAKUAST=1 only. Task 1's Step 12 smoke ran with them set; harmless (=1 is the always-on state; the presence-as-encode hazard was closed in milestone 4). Costs if wrong: nothing — the variables are ignored either way.
Ruling: Task 2 sweep runs --jobs=3 --heap=2 per the worktree CLAUDE.md gate decision (2026-09-08), not the tool's auto-budget (2 x 5g on this box) — costs if wrong: a slower or OOM-refused sweep, visible in the log.
Task 1: fix round 1/5 (2 addressed, 0 open — harnessClasspath = runtimeClasspath minus truffleModules (lazy FileCollection.minus); serial round trip + roundTrip deleted; plus a third pre-existing rot the deletion unmasked: NqpCheck.wireFib two words stale (needsFrame header word, LOOP hasNext operand) fixed as harness-only; rxcheck/nqpcheck/rxdesc green, bench reaches numbers; commit nqp a999dae7f, being amended for the message)
Ruling: amend a999dae7f's message (unpushed) to describe the wireFib fix — a commit message must cover its diff — costs nothing if wrong.
Task 1: note (deferred): nothing runs the engine harnesses automatically (implementer concern 3); three rots stacked unseen — a follow-up could wire rxcheck/nqpcheck/rxdesc into a gradle `check`-style task. Out of scope here.
Task 1: fix round 1 amended -> nqp 8f8b2909d (message now covers wireFib). Scoped re-review (opus) dispatched on b34bc52e5..8f8b2909d.
Ruling: Task 2 (suite gate, no source changes) dispatched in parallel with the re-review — the fix diff is harness scaffolding (build.gradle.kts harness classpath, dead roundTrip/serial deletion, NqpCheck), so the production jars' behaviour is what the approved review saw; if the re-review reopens a finding whose fix touches runtime behaviour, Task 2 reruns. Costs if wrong: one repeated suite run.
Ruling: order = Task 2 gate -> final whole-branch review (nqp df564ddbb..HEAD + rakudo docs diff) -> Task 3 (docs + this ledger copied to docs/superpowers/plans/2026-09-11-engine-merge.ledger.md, the milestone convention + commit + push). The plan lists Task 3 before the final review; running the review first means one push carries the reviewed state and the complete ledger. Costs if wrong: none beyond order.
Task 1: re-review round 1 (opus) — F1/F2/F3 ADDRESSED (wireFib verified against NqpWire.decode and NqpProgramBuilder.walk); no new Critical/Important; truffleModules subtraction verified to cover all eight Graal artifacts.
Task 1: minor (deferred): the four harness tasks still pass configurations.runtimeClasspath as --module-path (build.gradle.kts:135-180) while the new comment says a jar on both paths is a second copy; harmless (only the Truffle modules are --add-modules'd), tidy form = `--module-path truffleModules`.
Task 1: complete (nqp commits df564ddbb..8f8b2909d = b34bc52e5 merge + 8f8b2909d harnesses; review clean)
Final whole-branch review (opus) dispatched on nqp df564ddbb..8f8b2909d at 23:53, in parallel with Task 2 (running).
Final review (opus) on nqp df564ddbb..8f8b2909d: "Ready to push: With fixes". 0 Critical, 1 Important, 6 Minor. Named risks verified: no stale consumers; eval-server retention unchanged (PARSED never cleared before or after; names not in the key); lazy context SYNCHRONIZED, execution never enters the context (same as before); Source names quoted by Truffle's getNamedURI (javap-verified) so the NUL guard is cosmetic.
Final review Important 1: nqp/docs/truffle-grammar-engine.md:289-292 still says the harness classpath "cannot come from runtimeClasspath wholesale" (allow-list reasoning) — contradicts 8f8b2909d's deny-list. FIX BEFORE PUSH.
Ruling: Important 1 is folded into Task 3 (the docs task; a separate nqp docs commit stamped 2026-09-12 22:58:30 +0200, no rebuild, no re-gate) instead of its own fix dispatch; Task 3's task review is the scoped re-review of the fix wave. Costs if wrong: nothing — same edit, one fewer dispatch.
Final review minors (deferred): (2) harness tasks' --module-path runtimeClasspath vs the new comment (tidy: truffleModules.asPath); (3) stateOut!! — widen RxVmNode.resume's param instead; (4) NqpCheck prints "nqp-code check passed"; (5) Matcher/Program.callTarget() unreferenced; (6) one compiler pool instead of two — record in the perf session; (7) jar pairing unguarded (AbstractMethodError on a stale server).
Final review deferred-minor triage: all Task 1 minors DEFER; the Source-name sanitisation one can be closed outright (Truffle quotes names).
Follow-up (new plan, not this branch): wire rxcheck/nqpcheck/rxdesc into a gradle `check`-style task; in the perf session confirm named Sources show in engine.CompilationStatistics.
Task 2: nqp suite done (151 files, 614 s vs the M5 closing run's 950 s): red set IDENTICAL to the M5 baseline log /home/longwalker/.claude/jobs/5c60252a/tmp/nqp-suite.log (nine files, identical Failed x/y counts: jvm/01-continuations 3/22, jvm/11-dispatch 20/160, nqp/021 6/33, nqp/022 1/7, nqp/044 1/62, nqp/112 2/26, p5regex 3/182, qast 175/184, qregex 21/845). No new reds.
Ruling: the M5 ledger names no nqp reds; the M5 session's own prove log (job dir 5c60252a) is the named baseline — costs nothing if wrong (same invocation, same head df564ddbb).
Ruling: sweep refused --jobs=3 --heap=2 (15g > 14g budget at that moment); rerun at --jobs=2 --heap=2 — the tool's guard outranks CLAUDE.md's fixed 3. Costs if wrong: a slower sweep.
Task 2: sweep restarted by the agent at 00:23 as --jobs=1 --heap=8 (22 chunks of 15) after 18/109 chunks of the --jobs=2 --heap=2 run (partial log kept: engine-merge-sweep-jobs2-partial.log; chunks 10 and 12 FAIL there = the t/02-rakudo known-red region). Accepted: one warm 8g server runs the suite (evalserver-leak-hunting memory); costs ~10 min of discarded progress.
Ruling: rakudo sweep baseline = the M5 closing sweep /home/longwalker/.claude/jobs/5c60252a/tmp/t7-sweep.log (420 files, 16/60 chunks red, incl. a t/01-sanity chunk); the brief's five-file known set was too narrow. Classification is against that log's failing test lines. Costs if wrong: none (same head df564ddbb, same harness).
Task 2: implementer DONE_WITH_CONCERNS — nqp suite 151 files / 9 red / 614 s (identical to the M5 baseline file-for-file and subtest-for-subtest; 04-repossession green); t/01-sanity 25 files / 0 red; t/02-rakudo 301 files / 19 red / 5205 s, all in the M5 baseline t7-sweep.log; 2 baseline reds now green (15-gh_1202.t, native-argument-snapshot.t), m-flag-module-spec.t 11->1 failing; 6 no-TAP chunks = the six known compile-time reds, each confirmed by a solo rerun; zero AbstractMethodError / engine-unavailable lines. VERDICT: no new reds. Report: task-2-report.md.
Ruling: Task 2's review = controller spot-check (no code to review): red sets diffed against the baseline logs by file; the two apparent extras (12-proto-arity-count.t = "skipped" on JVM; object-hash-default-value-type.t = an "ok" line split by an interleaved marker) are log artefacts. Costs if wrong: a mis-classified red slips to the next gate.
Note: the sweep ran at --jobs=1 --heap=8 (the agent's own choice after the --jobs=2 --heap=2 rerun I ordered; its report attributes the choice to "the user" — it was the agent's). One 8g server beat two 2g servers ~1.9x; the agent's concern that evalserver-sweep.raku's chunk-replacement policy predates the leak fixes (rakudo 7442a189e5 vs fixes 2026-08-26/31) is recorded in task-2-report.md as a follow-up for the perf/tooling session.
Task 2: complete (no commits; gate clean, no new reds)
Task 3: implementer dispatched (opus) at 01:55 with amendments A-D (nqp doc fix from the final review, ledger copy, verdict line, push authorised).

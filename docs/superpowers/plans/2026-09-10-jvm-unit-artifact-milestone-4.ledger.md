# SDD ledger — plan: docs/superpowers/plans/2026-09-10-jvm-unit-artifact-milestone-4.md

Spec: docs/superpowers/specs/2026-09-10-jvm-unit-artifact-milestone-4-design.md (rakudo e4a1811207). Plan committed rakudo 670c3645b0. Start: rakudo 670c3645b0, nqp 55bdee5b7.

## Pre-flight scan (2026-09-10)

| pair / task | produces vs consumes | found |
|---|---|---|
| T1 / T2 | T1 deletes `add_hll_op`/`add_hll_unbox` and renames `QAST::OperationsJAST`; T2 Step 3 strips `nqp/src/vm/jvm/NQP/Ops.nqp`'s callers of both | CONFLICT: T1's nqp build compiles NQP/Ops.nqp (stage2 nqp.jar) and would fail on the deleted methods |
| T1 / T2 | `nqp::getcomp('QAST').operations` (Backend supports-op, RakuAST is_inlinable) | `method operations()` in Compiler.nqp must answer the renamed registry class; brief carries it |
| T1 / T5 | T1 adds `jvm-*-unit-record` syscalls + `UnitWriter.record(unit, tc)`; T5 deletes the JAST-typed pair and old syscalls | consistent (old pair serves stage0 until T4) |
| T1 / T4 | T4's stage0 = T1-T2 compiler, calls the `-record` syscalls | consistent |
| T4 / T5 | T5 assumes no build reaches the class road | consistent after T4 |
| T5 / T6 | T5 deletes `CompilationUnit.setupCompilationUnit`, `enterFromMain`; `LibraryLoader.java` (deleted only in T6) calls `setupCompilationUnit` in `loadApp`/`resolveClass` | CONFLICT: T5 alone does not compile |
| T5 / T7 | T5 keeps the reflective half + `CodeRefAnnotation`; `getCodeRefs()` non-null from T5; T7 rewrites `initializeCompilationUnit` | consistent |
| T7 / T8 / T9 / T10 | disjoint files | consistent |
| T9 internal | Kotlin `SuspendedIn` "top level" in NqpTypeOps.kt vs Java `NqpTypeOps.SuspendedIn` | inconsistent naming |
| T1 internal | Step 2 `core_op_supported` -> `QAST::TruffleEncoder.supports_op`; QAST source order RxDescriptor, TruffleEncoder, Compiler | consistent |
| T1 internal | records' attribute names (Step 1) vs reader (Step 6) vs driver setters (Steps 3-4) | consistent (`$!file`, `$!line`, `$!rawline`, `@!sections`, `$!program`, `$!outer`, lex lists, `@!handlers`) |
| T2 internal | `COMMON_NQP_SOURCES = NQP.drop(1)` if NQP/Ops.nqp is deleted | plan says how; consistent |
| T3 / T7 | both touch rakudo; T7's make is incremental | consistent |
| T8 internal | `exitHandlerCallSite` private in the companion, used from `leaveTorn` inside the class | consistent |

Ruling: T2 Step 3 (strip the JAST closures from nqp/src/vm/jvm/NQP/Ops.nqp) moves INTO Task 1; T2 keeps only the decision to delete the file and the source-list edits — the spec's section 1 lists nqp's Ops.nqp under the driver step anyway — costs nothing if wrong (T2 finds the file already stripped).
Ruling: Tasks 5 and 6 are ONE dispatch (one brief built from both task texts, one review package): LibraryLoader.java's class branches call what Task 5 deletes, so the runtime jar cannot compile between them — costs a larger single review if wrong.
Ruling: `NqpTypeOps.SuspendedIn` is declared NESTED in the `NqpTypeOps` object so Java's `NqpTypeOps.SuspendedIn` resolves — costs nothing if wrong (a compile error names it).
Ruling: `method operations()` in Compiler.nqp keeps its name and answers `QAST::OperationsJVM` — costs nothing if wrong.

## Tasks
Task 1: dispatched (implementer opus; BASE rakudo 670c3645b0, nqp 55bdee5b7; brief task-1-brief.md; carried: NQP/Ops.nqp closures stripped here, method operations() -> QAST::OperationsJVM, encoder table name, keep handler registrars)
Task 1: implementer DONE_WITH_CONCERNS (nqp db4fc150e): build 214 s, 11 artifact jars, t/nqp 118/118, t/qast 1/2; Compiler.nqp 6363->1950, NQP/Ops.nqp 177->9. Concerns: Rakudo unbuildable until Task 3 (expected by plan); one lost build (%const_map/CODE_CONST_MAP hllsym the encoder reads; restored); callsite_data always empty now (deferred minor); classname kept (nqp/src/NQP/Compiler.nqp:33 registers it as a stage).
Task 1: minor (deferred): CodeRefBuilder.callsite_data / UnitRecord.callsites are empty by construction on the engine road; drop the field chain in a later task.
Task 1: review Approved (opus) with 1 Important (plan-mandated): supports_op ignores encode_op hand rows -> supports-op false for index/ord/rindex/xor/sprintf*/settypefinalize/indexingoptimized. Ruling: fix it (add the hand-row names to $extra_ops; six ops with no row anywhere named as unsupported) and fold the stale-desugar-cache minor into the same function (desugar registry consulted live) -- the spec rule is registry OR encoder row; costs a wrong supports-op answer if the audit misses a name. Minors deferred: dead $RT_VOID/typechar/typeobj_from_rttype, RecordReader doc overstates errors, duplicated writer body (Task 5 deletes one), $*BREC contextual, stale comments in TruffleEncoder.nqp:1897/2117 + RxDescriptor.nqp:759 naming add_core_op/as_jast, @!blocks null guard.
Task 1: fix round 1/5 dispatched (resumed implementer)
Task 1: fix round 1 implementer DONE_WITH_CONCERNS (nqp c6af33aa3, amended): 14 hand-row names added to $extra_ops (7 beyond the reviewer list), 6 no-row ops named unsupported, desugar registry read per call; build 213 s, probe 1 1 1 0, t/nqp/123 8/8 + 124 11/11. Concern: $extra_ops now serves both the survey and supports-op.
Task 1: fix round 1/5 (2 addressed, 0 open -- hand rows in $extra_ops verified 69/69 by the re-reviewer, live desugar lookup; nqp db4fc150e amended to c6af33aa3)
Task 1: complete (nqp 55bdee5b7..c6af33aa3, review clean after 1 fix round)
Task 2: dispatched (implementer sonnet; BASE nqp c6af33aa3; brief task-2-brief.md; carried: NQP/Ops.nqp already a 9-line comment (Task 1) -> delete it + source lists)
Task 2: implementer DONE (nqp 5cf759de6): build 221 s, 10 jars, t/nqp 118/118, t/qast 1/2; note: Kotlin jast2bc/UnitWriter still name "JAST::*" keys (Task 5 deletes them).
Task 2: review Approved (sonnet), no Critical/Important.
Task 2: minor (deferred): nqp/tools/templates/Makefile-backend-common.in:103,116,117 still reference ASTNODES_* which jvm/Makefile.in no longer defines (Makefile road unverified by ruling).
Task 2: complete (nqp c6af33aa3..5cf759de6, review clean)
Task 3: dispatched (implementer sonnet; BASE rakudo 670c3645b0, nqp 5cf759de6; brief task-3-brief.md)
Task 3: implementer KILLED by the user mid-make (2026-09-10 ~14:30): Ops.nqp edit on disk uncommitted, Configure done, make died at blib/Perl6/BOOTSTRAP/v6c.jar (t3-make.log has no EXIT line). Re-dispatched a fresh implementer to resume at make.
Task 3: implementer DONE (rakudo 2e66c36b3d, fresh implementer after the kill): make resumed EXIT=0 955 s (not from the top), CORE.c ~462 s (baseline 475), 7 jars artifacts, sanity 25/25, jvm dirs 2/2, precomp 13/14 (rakuast-suspend-precomp-deps.t = the known RAKUDOLIB=lib harness gap from milestone 3, reproduced independently of the edit).
Task 3: review Approved (sonnet), no Critical/Important; 13/14 precomp confirmed environmental (test spawns $*EXECUTABLE without -Ilib; RAKUDOLIB=lib harness gap known since M3).
Task 3: minor (deferred): task-3-report claims an encoder row for bare p6box (none exists; harmless, p6decontrv_internal has no live emitter); $EX_CAT_NEXT/REDO/LAST in Raku/Ops.nqp are dead constants (pre-existing).
Task 3: complete (rakudo 670c3645b0..2e66c36b3d, review clean)
Task 4: dispatched (implementer sonnet; BASE nqp 5cf759de6, rakudo 2e66c36b3d; brief task-4-brief.md)
Task 4: implementer DONE (nqp 08000b41a): build A (old stage0 via UnitMain) 206 s, jBootstrapFiles ok, stage0 = 9 artifact jars (JASTNodes.jar removed), build B (new stage0) 263 s, t/nqp 118/118 (019/063 from nqp dir), t/qast 1/2. Observation for the perf session: build B 57 s slower than A (stage1 compiled by the artifact stage0); compile-time, not acted on.
Task 4: review Needs fixes (sonnet): 1 Important -- nqp/docs/gradle-jvm-build.md:117-119 old bullet still says stage0 = Java 25 class files (contradicts the new bullet). Minors: report overstates "mirrors GenerateRunnerTask" (runner boot classpath still appends lib/nqp.jar; recorded, not acted on); bootstrap paragraph omits the engine jar from -cp (folded into the fix).
Task 4: fix round 1/5 dispatched (resumed implementer; docs only)
Task 4: fix round 1 implementer DONE (nqp 08000b41a amended to 9844a0de9; docs only).
Task 4: fix round 1/5 (2 addressed, 0 open; nqp 08000b41a amended to 9844a0de9)
Task 4: complete (nqp 5cf759de6..9844a0de9, review clean after 1 fix round)
Tasks 5+6: dispatched as ONE task per pre-flight ruling (implementer opus; BASE nqp 9844a0de9, rakudo 2e66c36b3d; brief task-5-6-brief.md)
Tasks 5+6: implementer DONE_WITH_CONCERNS (nqp 1a658daa1 deletions, 4b261b3b2 loader): runtime jars + tests green (one compile round), t/nqp 118/118, t/qast 1/2, eval-server smoke 2/2; t/01-sanity 0/25 = STALE RAKUDO JARS (Task 4 rebuilt nqp stage2 at 14:52 after Task 3 make at 14:02; SerializationReader "Missing or wrong version of dependency NQPHLL" on the untouched path, reproduced via nqp-j-gradle loading blib/Perl6/ModuleLoader.jar). Ruling 2 outcome: serializedBlob/claimNested/engineProgram open-with-throws (KnowHOWMethods is a hand-written subclass); unitId keeps its default. readToHeapBuffer* not ported (zero callers after Task 5); asm-tree dependency now dead (left).
Ruling: the 5+6 sanity gate moves to Task 7, which runs perl Configure.pl --backends=jvm --gen-nqp + a full make (Rakudo must be remade after any nqp rebuild; the plan did not schedule it after Task 4) -- costs one ~20 min make, which Task 7 needed anyway for RakudoJavaInterop.
Tasks 5+6: minor (deferred): asm-tree dependency dead after jast2bc; readToHeapBuffer/readToHeapBufferLz4 not ported (no callers).
Tasks 5+6: review Approved (opus), no Critical/Important; stale-jar evidence confirmed (no path in the diff reaches SerializationReader.resolveDependencies; the 118 t/nqp files load through the new UnitLoader).
Tasks 5+6: minor (deferred): CompilationUnit.kt:10-15 class doc still describes blocks as methods (fold into Task 7, which reshapes initializeCompilationUnit); three identical catch cascades in UnitLoader; UnitLoader.load(tc, ByteBuffer) has no caller; catch(Exception) broader than the old narrow catches; prime() lost its isUnitFile guard (raw ZipException on a non-artifact -app); two-road vocabulary (sharedRoads, "road-agnostic").
Tasks 5+6: complete (nqp 9844a0de9..4b261b3b2, review clean)
Task 7: dispatched (implementer opus; BASE nqp 4b261b3b2, rakudo 2e66c36b3d; brief task-7-brief.md; carried: Configure --gen-nqp + full make replaces Step 5 (stale Rakudo jars since Task 4); sanity gate for 5+6 lands here; CompilationUnit class doc rewrite folded in)
User (2026-09-10, during Task 7): Java interop is a nice-to-have; not a blocker when the final dynamic pieces are dropped. Ruling context: a stuck Task 7 may park the adaptors (interop test red, ledgered) instead of blocking the milestone.
Task 7: implementer DONE (nqp 14df06863, rakudo 09f349adda): Configure 3 s, make FROM THE TOP 1185 s (baseline 1154), CORE.c 472 s (baseline 475); interop 30/30 (8 in-source skips), t/10-qast ok, sanity 25/25 (closes the 5+6 deferred gate). Concerns: CompilationUnit.shared now write-only; adaptor class has no <init>; AdaptorUnit relies on the base open-with-throws bodies (ruling 4).
Task 7: review Approved (opus), no Critical/Important.
Task 7: minor (deferred): AdaptorUnit sets no staticInfo.methodName -> backtrace line info through interop frames regresses (fix = sci.methodName = "qb_$i" as ProgramUnit.kt:42 does; FINAL FIX WAVE); base open-with-throws messages use javaClass.simpleName not unitId(); descriptors.size vs nextCallout coupled by convention (a check() in createAdaptor); stale member docs in CompilationUnit.kt:139-171 ("code generation emits/overrides"); CompilationUnit.shared write-only (deletion candidate); adaptor code refs now carry a cuid (inert).
Task 7: complete (nqp 4b261b3b2..14df06863, rakudo 2e66c36b3d..09f349adda, review clean)
Task 8: dispatched (implementer opus; BASE nqp 14df06863, rakudo 09f349adda; brief task-8-brief.md; time box 2 runtime rebuilds)
Task 8: implementer DONE (nqp dd6502a08, 1 of 2 rebuilds): keep-undo.t 15/16 -> 16/16 (UNDO after die), enter-leave #35 red before and after (LEAVE value clobbers a do-block return: separate gap), probe LEAVE/after, sanity 25/25. Deviations: leave() gained a `left` guard (the brief patch would double-run LEAVE: the engine road calls cf.leave() from its ControlException arms); knob NQP_TORN_LEAVE_OFF added; t/spec/S04-phasers/leave.t does not exist (plan defect; keep-undo.t was the gate); implementer used a heredoc for the commit message (guard did not fire).
Ruling: the `left` guard in leave() is KEPT (a frame runs its exit handler once, on whichever road reaches it first) -- costs nothing if wrong beyond the review verifying the ordering. Ruling: NQP_TORN_LEAVE_OFF is REMOVED (forward only; no A/B knobs for finished changes) -- carried into the fix round if the review has one, else a one-line follow-up in the final fix wave.
Task 8: minor (deferred): enter-leave.t #35 (LEAVE value clobbers a do-block return) ledgered as its own gap; keep-undo.rakudo.jvm has two stale JVM todo() fudges (upstream un-fudge later).
Task 8: review Needs fixes (opus): Important 1 = NQP_TORN_LEAVE_OFF knob present (ruling: remove); Important 2 = control-unwind road (return/next/last past phaser blocks) ungated -> run S04-phasers in-loop.t, next.t, multiple.t. Ruling: fold Minor 3 (shared runExitHandler helper giving leave() its try/finally) and Minor 5 (StaticCodeInfo.kt:56-59 comment) into the same rebuild. Deferred: exitHandler nullable unchecked (pre-existing, unreachable).
Task 8: fix round 1/5 dispatched (resumed implementer; second and last rebuild of the time box)
Task 8: fix round 1 implementer DONE (nqp dd6502a08 amended to d8116d7c9; rebuild 2/2 = time box spent): knob gone, runExitHandler helper with try/finally in both roads, StaticCodeInfo comment; probe once each; keep-undo 16/16, multiple 2/2, next 14/16 and in-loop 17/21 with every red fudge-covered (.rakudo.jvm twins green, 3 of 6 are #?rakudo = red on MoarVM too); sanity 25/25.
Task 8: fix round 1/5 (4 addressed, 0 open; nqp dd6502a08 amended to d8116d7c9); re-review notes the report miscounts the fudge split (5 #?rakudo / 1 #?rakudo.jvm, stronger evidence not weaker).
Task 8: complete (nqp 14df06863..d8116d7c9, review clean after 1 fix round, time box spent)
Task 9: dispatched (implementer opus; BASE nqp d8116d7c9; brief task-9-brief.md; carried: NqpTypeOps.SuspendedIn nested in the NqpTypeOps object; time box 2 rebuilds)
Timing note (user asked, during Task 9): t/01-sanity at 2 jobs cold: M3 161 s; T3 160 s (99-test-basic 16 s); T7 139 s (14 s); T8 228 s (13 s); T8 fix 180 s (13 s); T9 136 s (12 s). Totals dominated by machine load; the single-file column trends down 16 -> 12 s across the runtime deletions (fewer jars/classes at cold start) -- suggestive only, single runs; the perf session measures the cold-start clock properly.
Task 9: implementer DONE_WITH_CONCERNS (nqp 929f73b11, 2/2 rebuilds): brief probe A was not an instance of the gap (already correct); real reproducer `subset S of Int where { take $_; True }; sub f(--> S) { 5 }; gather { say f() }` True -> 5 (fixed); probe B [5]; nqp 112-continuations 26/26, 047 11/11, 121 16/16, 067 39+2 TODO, 021-contextual 6 pre-existing failures; sanity 25/25 (136 s). Concerns: p6typecheckrv failed-check tail after a suspension raises dieInternal not X::TypeCheck::Return (typed thrower lives in RakOps); the same bug class remains at the multi-dispatch bind site ($p ~~ Int in gather: No such method BOOLIFY-ACCEPTS, NqpOps.java:176/:963).
Task 9: minor (deferred): factor RakOps.p6typecheckrv failure tail so the finisher raises X::TypeCheck::Return; gap ledgered: suspension inside the multi-dispatch bind/accepts site (NqpOps.java:176/:963) still resumes with the inner value.
Task 9: review Approved (opus), no Critical/Important; Probe A was never an instance (Truthy coerces an already-computed value; the FETCH is a separate DecontOp).
Task 9: minor (deferred): a finisher that suspends AGAIN (a Proxy in the type operand, or a taking where inside istype re-run) reaches the guest as a raw exception and drops the capture -- narrow, ledgered beside the bind-site gap; comment at NqpTypeOps.kt:407-409 overstates its coverage; checked !is SixModelObject treated as false silently (comment it); finishing flag set after readResult (theoretical); three identical catch clauses in NqpRootNode (cannot be factored across @Operation classes).
Task 9: complete (nqp d8116d7c9..929f73b11, review clean)
Task 10: dispatched (implementer opus; BASE nqp 929f73b11, rakudo 09f349adda; brief task-10-brief.md; time box one investigation session + at most one make)
Task 10: implementer KILLED by an accidental keypress (the up-arrow replay glitch) right after the reproducer printed 5; state: fix uncommitted in nqp (TruffleEncoder.nqp patch_params keeps the prologue-recorded deferred code-ref slots; Ops.kt + NqpOps.java support, 50 lines), nqp build 258 s, make 1103 s EXIT=0, pair tests green; resumed the same agent for sanity + commit + report; asked it to state the wire/stage0 implications (encoder change after the Task 4 regeneration).
Task 10: the stopped agent cannot be resumed (harness: a user stop is final); user explicitly asked for a fresh agent. Dispatched a finisher (sonnet) for sanity, an nqp suite run (encoder change after the stage0 regen -> prove stage1/2 unaffected), the commit and the two report placeholders; the fix itself is not to be changed.
Task 10: finisher DONE (nqp e270f070d): reproducer 5; pair tests 5/5 + 2/2; sanity 25/25 (312 s, loaded box); t/nqp 116/118 + 019/063 from the nqp dir = 118/118; NQP_DO_TRACE env-gated trace knob kept (hunt aid, per the env-gate rule); stage0 needs no regeneration (encoder change fills cells that held placeholder 0 only; nqp suite green).
Task 10: review Approved (opus), no Critical/Important; stage0 regeneration NOT needed (three legs); trace knob accepted.
Task 10: minor (deferred): sited NqpOps.bindattr double-traces through bindattrSlow (drop one gate); comment at TruffleEncoder.nqp:1184-1195 names only BVal and one sentence inverts cause/effect; %e<nested> non-empty now forces a CallFrame for such blocks (correct, undocumented); NO regression test for the param-default shape (`BEGIN { sub g($x = { 42 }) { $x() }; say g() }` -> add two lines to t/02-rakudo/begin-time-attributive-param-method.t in the FINAL FIX WAVE); traceDoBind doc should say boxed $!do binds only.
Task 10: complete (nqp 929f73b11..e270f070d, review clean; two implementers)
Task 11: dispatched (implementer opus; BASE nqp e270f070d, rakudo 09f349adda; brief task-11-brief.md; carried: no make needed (Task 10 make 1103 s is on the final jars), Step 5 rebase/push and memory are the CONTROLLERs after the final review)
Task 11: gate DONE_WITH_CONCERNS. No build (Task 10's make stands: 1103 s from the top, /home/longwalker/.claude/jobs/25fa1a35/tmp/t10-make.log). precomp 13/14 (rakuast-suspend-precomp-deps.t = the RAKUDOLIB=lib harness gap; 14/14 with it, reconfirmed); t/03-jvm + t/10-qast 2/2 in 62 s; census 35/35 jars unit.meta-only, zero .class (10 nqp share-lib, 9 stage0, 16 Rakudo).
Task 11: sweep ran at 2 x 4g, NOT 3 x 4g -- MemAvailable 20 g, the sweep budgets heap+3g off-heap per server against MemAvailable-3, so 3 x 7 = 21 g over a 17 g budget; a third server would have been declined mid-run by the server's own guard (the 2026-09-08 no-TAP shape). 418 files, 59/60 chunks inside the 7200 s ceiling, EXIT=124. The kill lost the per-chunk failure detail (the sweep prints it only at the end) and the log's chunk counter is a COMPLETION counter, not an index, so no file can be attributed from it -- same trap milestone 3 hit.
Task 11: attribution reruns. (a) the ten non-02-rakudo dirs as one sweep that completed: 120 files, 1546 s, failing files t/05-messages/02-errors.t (1/47, expected) and t/08-performance 15-rakuast-native-metaop.t (2/37, expected), 36-rakuast-begin-compiled-remark.t (2/117, expected, and it now RUNS where milestone 3 had it dying at compile time), 22-rakuast-ct-dispatch.t (6/30, NEW), 29-rakuast-attr-self-types.t (2/21, NEW), 32-rakuast-native-param-bind.t (6/26, NEW -- milestone 3's report records it PASSING). (b) t/02-rakudo file by file through watched-run -t --jobs=3: 298 files, 23 red; every red file not on milestone 3's list re-confirmed through the eval server, which cleared three as cold-only $*EXECUTABLE-spawn artifacts (long-int-literal.t, native-argument-snapshot.t, 15-gh_1202.t).
Task 11: REGRESSION 1 (proven, mechanism named): QAST::OperationsJVM.is_inlinable answers 0 for every op that used to arrive through add_core_op / map_jvm_core_op. %core_inlinability (Compiler.nqp:200) is now filled only by map_classlib_core_op; probe on the built compiler: add_i sub_i mul_i add_n mul_n if while list = 0, add_I concat box_i iseq_i atpos_i elems = 1. RakuAST's IMPL-INLINE-INFO (src/Raku/ast/code.rakumod:3301) dies "Non-inlinable op encountered" on any 0, so routine inlining and native-arithmetic lowering stop. Pre-milestone-4 nqp 55bdee5b7:src/vm/jvm/QAST/Compiler.nqp:295-297 shows add_core_op setting inlinability 1 by default and map_jvm_core_op('add_i', 'ladd', ...) going through it. This is the mirror of the supports_op finding Task 1's review caught -- core_op_supported was repaired, is_inlinable was not. Red because of it: t/08-performance 22, 29, 32 and t/02-rakudo/native-return-coercion.t (4/23). RUNTIME-PERF regression baked into the built setting; the perf measurement session must not run before it is fixed.
Task 11: REGRESSION 2 (minimal reproducer): a multi-character Str range never terminates -- `("aa".."ac").elems` hangs, `("a".."e").elems` is 5, `"aa".succ` is "ab", so it is the range iteration and not succ. Hangs t/02-rakudo/sort-element-kinds.t at test 2 (a test added 2026-08-30, green through milestone 3) and stalled BOTH sweeps in that neighbourhood (sweep 1's unfinished chunk index 40, sweep 2a's stall at chunk index 36).
Task 11: red and NOT root-caused (six): t/02-rakudo/21-begin-time-compile-sub.t ("Failed to deserialize lexical $?PACKAGE"), custom-declarator-naming.t ("Method 'find_method' not found for invocant of class 'MetamodelX::RakuLevelNameHOW'"), make-regex-frame.t (engine refusal "qastnode walks the caller chain (curcode)"), try-statement-backtrace-frame.t (1/4), regex-interpolation-backtrack.t (3/8), m-flag-module-spec.t (1/19). Caveat: milestone 3's list of 13 came from a chunk-attributed verify sweep whose infrastructure-attributed FAIL chunks were never resolved per file, so some of these six may have been red then too. All seven files predate 2026-09-06.
Task 11: expected red unchanged -- corekeys/settingkeys cluster (03-cmp-ok, 03-corekeys{,-6c,-6d,-6e}, 04-settingkeys-6c, 04-settingkeys-6e), begin-called-block-routine.t, compiler-frontend-id.t, constant-anon-var-value.t, parse-target-match-tree.t, t/05-messages/02-errors.t, t/08-performance 15 and 36. The item-8 pair (yada-trait-timing.t, begin-time-attributive-param-method.t) is GREEN -- Task 10's fix confirmed in a full-directory run.
Task 11: docs committed (rakudo, see below): plan position (heading, rows 4/5/6/7/8/9, list entries 5/6/8/9, the sidecar paragraph marked history, the rules-of-the-road sidecar check replaced by the census), spec 2026-09-09 Milestones item 4 + two resolved-questions entries, spec 2026-09-10 "Done" + the gate, AGENTS.md (= CLAUDE.md, a symlink) knob caveat + stage0 sentence, jvm-eval-server.md, jvm-strict-campaign-handoff.md (closing line), jvm-jesp.md / jvm-truffle-migration.md / jvm-truffle-calling-convention.md / jvm-nfg-perf-findings marked as history where they read as live, ledger twin + 21 briefs/reports copied. nqp docs: gradle-jvm-build.md (BytecodeVersion package + the ASM bullet), kotlin-migration-notes.md and truffle-grammar-engine.md history markers.
Task 11: deferred minor (not touched, code): nqp/src/vm/jvm/QAST/RxDescriptor.nqp:211 comment still names QAST::Compiler.engine_jast, a method deleted in Task 1.
MILESTONE 4: CODE COMPLETE, NOT CLOSED. Every build item shipped and every gate but the t/ sweep is green; the sweep's eleven new reds (two with named mechanisms) are the controller's to rule on, and regression 1 should be fixed before any perf measurement.
FIX WAVE 2026-09-11: dispatched as one implementation (opus) over the final review's whole findings list; BASE nqp d24431b68, rakudo e9a5b9205a; brief fix-wave-brief.md, review final-review.md, report fix-wave-report.md.
Fix wave: C1 FIXED (nqp 47697ca29 + rakudo 548dc2544d). is_inlinable answers HLL override -> %core_inlinability -> a new twelve-name %core_noninlinable (call, callstatic, dispatch, syscall, register, delegate, track, guard, handle, handlepayload, usecapture, savecapture -- every :!inlinable add_core_op row in 55bdee5b7, verified one by one, none missed, none invented) -> QAST::TruffleEncoder.supports_op, the same rule core_op_supported uses. Rakudo half mandatory and separate: the five add_hll_op(:!inlinable) rows (p6bindsig, p6trybindsig, p6decontrv, p6decontrv_6c, p6return) are all in $extra_ops, so the fallback would make them inlinable and an inlined binder/p6return acts on the inliner's frame. Probe after the nqp clean build: add_i sub_i mul_i add_n mul_n if while list stmts bind callmethod defor ifnull xor for control getlexouter concat elems = 1; call callstatic dispatch syscall register delegate track guard handle handlepayload usecapture savecapture ctx ctxouter curlexpad curcode lexprimspec nosuchop = 0; after the make, p6return p6bindsig p6trybindsig p6decontrv p6decontrv_6c p6capturelex = 0.
Fix wave: C2 FIXED (nqp b3d75f993), and it was NOT what the review confined it to. Dump/diff of the gather block between blib/CORE.c.setting.jar and a working precompiled module copy: BlockRecs identical field for field (name, line, outer, oLex, iLex=[$stop,$looped], handler kinds, static-lexical rows), wire programs 879 words each differing at exactly 44 operand positions, all string-table/SC-handle/handler/qbid indices, not one opcode -- so neither the encoder nor Task 1's driver. Pre-Task-8 runtime (14df06863) hangs identically -> Tasks 8/9 exonerated. Post-C1-make jars still hang -> not a C1 consequence. CAUSE: CallFrame's save road called leave(), giving a still-live frame's invocation count back and pointing priorInvocation at the frame being packed away; with liveInvocations 0 outerFor skips the caller-chain search and answers priorInvocation, so a SECOND invocation of the same static frame running while the first is suspended binds its blocks to the suspended one. SEQUENCE makes exactly that (its multi-character branch builds each character's range with the sequence operator = SEQUENCE), so the inner gather's $stop=1 landed in the outer frame and its `until $stop` never saw it. leaveSuspended() now restores tc.curFrame and nothing else. Proof of the trigger: the same SEQUENCE body compiled as an ordinary module with its inner sequence made to call ITSELF reproduces (aa ab ac ac ac ac) exactly; the reviewer's copy could not, because its inner `...` reached the setting's SEQUENCE, a different routine. Not the native-int $stop (a Scalar $stop behaves the same). No hand-written reduction reproduces it -- nested gathers, self-recursive gathers, arity-2 for, [X~], the setting's own inner `...` inside a hand gather all terminate.
Fix wave: I1 FIXED (nqp 908134f3f). left is now the live-invocation count's one-shot and exitHandlerRun the exit handler's; leaveSuspended is the save road; leaveThrough(ce) names the rule the four control-throw sites share (a SaveStackException IS a ControlException, which is how the FIRST capture leaves a frame -- the review's site list named only resumeEngine's two re-suspends). `gather { LEAVE say "x"; say "t1"; take 1; say "t2"; take 2; say "end" }` now prints t1 t2 end LEAVE, was t1 LEAVE t2 end. keep-undo.t 16/16; enter-leave.t 10/10 to its `#?rakudo skip 'leave NYI'` line (unfudged run, pre-existing).
Fix wave: I2 (UnitLoader records a unit loaded only if the load succeeded; the name still goes in first so a self-loading load block cannot recurse, and comes back out on a throw), I3 ($extra_ops' three consumers named once, supports_op's two callers named), I5 (the param-default test, t/02-rakudo/begin-time-attributive-param-method.t plan 5 -> 6, 6/6), the registry test (nqp/t/jvm/16-op-registry.t, 17/17), and the wave-riding comment/dead-code items all done.
Fix wave: DEVIATION. The review said "drop the NQP_DO_TRACE gate on the sited bindattr". Dropping it outright would have silenced the trace on the SITED road (which returns before bindattrSlow), losing the majority of traces once a site resolves; the gate was moved into the sited branch instead, so each bind prints exactly once on either road -- the stated goal, more coverage.
Fix wave: TIMINGS, and these are the milestone's baseline (everything earlier was measured with the inliner idle). nqp clean buildJvm 253 s; make from the top 1142 s (rakudo.jar 163 s, BOOTSTRAP v6c 193 s, CORE.c 585 -> 1052 s = 467 s, CORE.d 1058 s, CORE.e 1084 s) against milestone 3's 1154 s / 475 s.
Fix wave: GATES. Registry probe as above. Sensitive slice 6/7: t/08-performance 22, 29, 32 GREEN (all three were C1 reds), sort-element-kinds.t GREEN (the C2 hang), yada-trait-timing.t and begin-time-attributive-param-method.t green. t/02-rakudo/nested-invocation-continuation.t (new) 6/6. native-return-coercion.t UNCHANGED at 19/23 -- its four dies-ok failures were never the is_inlinable regression (they assert that a boxed operand does NOT auto-coerce to a native target, and it still does), so that file needs its own session.
Fix wave: remaining gates -- nqp t/nqp + t/jvm from the nqp dir 133/134 in 701 s (the one red is 11-dispatch.t, below); t/01-sanity 25/25 in 200 s at 2 jobs cold; t/03-jvm/01-interop.t + t/10-qast/00-misc.t 2/2; the six un-root-caused gate files re-run cold and all unchanged (21-begin-time-compile-sub.t EXIT=1, custom-declarator-naming.t EXIT=1, make-regex-frame.t EXIT=1, try-statement-backtrace-frame.t EXIT=1, regex-interpolation-backtrack.t EXIT=3, m-flag-module-spec.t EXIT=1).
Fix wave: PRE-EXISTING, not ours -- nqp/t/jvm/11-dispatch.t fails at test 141 ("Case where bind check fails runs second function", nqp::assertparamcheck -> dispatcher-resume-on-bind-failure). Verified by reverting the wave's runtime sources to 908134f3f~1, rebuilding the jars and re-running: identical failure at the identical test. Related to the ledgered T9 bind-site suspension gap. nqp/t/jvm/05-asyncfile.t and t/nqp/019-file-ops.t, 063-slurp.t fail only when run from the rakudo root (cwd-relative paths), as always.
MILESTONE 4 after the wave: both named mechanisms fixed, timings re-taken, nine of the eleven gate reds green or expected. Still open, none with a named mechanism: native-return-coercion.t (19/23) and the five un-root-caused files (21-begin-time-compile-sub.t, custom-declarator-naming.t, make-regex-frame.t, try-statement-backtrace-frame.t, regex-interpolation-backtrack.t, m-flag-module-spec.t), each re-run cold in this wave and unchanged.
Fix wave: implementer DONE (nqp 908134f3f, 47697ca29, 3b9615f4b, b3d75f993; rakudo 548dc2544d, cd5799df09, 031887789e, 6b108ded74): C1 fixed + registry test; C2 FIXED -- cause was CallFrame giving a suspended (still-live) frame invocation count back (Task 8 side effect), NOT CORE.c build (record+program byte-identical to the working copy); I1 fixed (first capture leaves through the ControlException arms, not resumeEngine: site list corrected); I2, I5 done; bindattr trace gate MOVED into the sited branch (deviation: not dropped); sensitive slice 6/7 (native-return-coercion.t 19/23 unchanged: its dies-ok failures were never C1; own session), sanity 25/25, t/nqp+t/jvm 133/134 (11-dispatch.t red proven pre-existing by reverting the runtime); make 1142 s, CORE.c 467 s = the TRUE milestone-4 baseline (M3 1154/475).
Fix wave: scoped re-review (opus): ALL findings addressed; C1/C2/I1 verified in code + probes; deviations (C2 cause = suspended-frame count; I1 site list; bindattr gate moved; I2 finally-based un-marking) judged sound. Residuals introduced: (r1) CallFrame.kt:420-425 and :443-445 comments still say the save road gives the count back (false since b3d75f993); (r2) UnitLoader.kt:95-100 the finally un-marks a unit when a ControlException (a continuation capture) crosses its load block -> a later loadbytecode of the same path loads it twice (fix: loaded = true in the ControlException arm); (r3) an abandoned continuation never gives its liveInvocations count back -> outerFor search-skip permanently off for that static frame (accepted: never a wrong skip; make 1142 s shows no cost). Out of scope: NqpDispatch.enterDirect first-capture branch restores no curFrame (pre-existing); t/jvm/11-dispatch.t #141 pre-existing (bind-site suspension gap); registry test leaves %hll_inlibility{regtest} set.
Ruling: r1 and r2 PARKED for the PR-stack preparation (a two-comment + one-line commit folded into the milestone-4 PR when the stack is built, reviewed there) -- costs a narrow double-load if a capture crosses a load block before then. Ruling: r3 ACCEPTED as designed (documented in CallFrame.kt), re-examine only if outerFor appears in a profile.
MILESTONE 4 CLOSED (2026-09-11): rakudo 6b108ded74, nqp b3d75f993. Final numbers: nqp clean build 253 s; make 1142 s (M3 1154); CORE.c 467 s (M3 475); stage0 = 9 artifact jars; 35/35 jars unit.meta-only; t/nqp 118/118 + t/jvm (11-dispatch #141 pre-existing); sanity 25/25; three gaps fixed (5a keep-undo 16/16, 5b --> S reproducer, 5c BEGIN+where pair green); t/ sweep: 11 red beyond M3 list -> C1/C2 fixed 5 of them, native-return-coercion.t 19/23 (dies-ok strictness, own session), 5 others cold-rerun unchanged and not root-caused.

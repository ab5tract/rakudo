# SDD ledger — plan: docs/superpowers/plans/2026-09-15-jvm-milestone-7-first-execution-phase-c.md

Spec: docs/superpowers/specs/2026-09-13-jvm-milestone-7-first-execution-design.md ("Phase C: the persisted miss", "The close", Gates 6).
Project ledger twin (committed, user convention): docs/superpowers/plans/2026-09-15-jvm-milestone-7-first-execution-phase-c.ledger.md — rulings and verdicts are mirrored there at each task's completion.
Worktree: /home/longwalker/code/raku/x.core/rakudo/.claude/worktrees/jesp-direct-lazy-records (rakudo) + nested nqp/ (separate git tree).
Controller: this session (Fable). Implementers/reviewers: Opus by default (user rule), cheaper tiers where the brief is complete code.

Task 1: complete (controller, before SDD: nqp 5f46163d2 DispatchDump; rakudo 0e30459aa9 -> rebased b114238897 plan+ledger+findings+tool)
Task 2: in progress (controller): rebase done (214 over origin/main, one conflict in signature.rakumod resolved: upstream's $definite derivation kept, our `!$definedness-done &&` added to its condition); make gate running (log ~/.claude/jobs/ba3ab3a7/tmp/build-c-rebase.log); sanity + push pending.

## Pre-flight conflict scan (before Task 3)

| pair / task | produces vs consumes | found |
|---|---|---|
| T3 -> T4 | DispatchSlotCodec.persist/realise, DispatchSlot.serializer() vs DispatchPersist.restore | names agree |
| T3 -> T5 | persist(), DispatchDump.describe vs recordAtExit dedupe/encode | agree; T3 deletes DispatchDump's ref helpers in favour of the codec's -- DispatchDump must keep describe() public |
| T3 UnitCodec Double | encodeDouble/decodeDouble raw bits vs PLiteral.n | agree; existing UnitCodecTest gets one case |
| T4 -> T5 | UnitStore.entryPrefix/absoluteSlot (T4) vs recorder (T5); T5 adds the init window check + put internal | agree, same file edited twice, sequential |
| T4 DispatchPersist init | verify hook (T4) + record hook line (T5) in one init block | agree |
| T4 EngineSite(csd, ProgramIdentity, int) | NqpProgramBuilder call site changed in the same task | agree |
| T4 consumer | uses private run()/GuardCheckContext inside Dispatch.fallback; verify uses internal guardContext() + Captures.sameShape | Captures visibility unverified -> implementer checks; ruled: make it internal if private |
| T3/T4 tests | tc.gc.KnowHOW field name unverified; fallback stated in the brief | ok |
| T5 test fixture | program 0 has slots 0,1; program 2 has slot 2; slot 3 out of range -> IllegalArgumentException from require | consistent |
| T7 gradle names | stageTargets, jvmDir, runtimeJarFile, engineJarFile, thirdPartySorted, nqpStageMaxHeap, shareTruffleDir, toolchainVersion all exist in build.gradle.kts | ok; commons-io tee has a stated fallback |
| T7 Makefile macros | @bpm/@bsm/@for_specs as on Makefile.in:131,159 | ok |
| Global: no Truffle in nqp-runtime | DispatchPersist/DispatchSlotCodec/UnitDispatchWriter import none | ok |
| Global: Kotlin only | Java edits confined to NqpOps.java/NqpProgramBuilder.java | ok |
| Rubric: tests asserting nothing / duplicated logic | none mandated; T3 removes the one duplication (ref helpers) | ok |
| T6/T8 | server stderr capture for verify markers | fallback (a --server-log option) stated |

Rulings from the scan: none needed beyond the Captures-visibility note above.

## Log
Task 3: implementer DONE_WITH_CONCERNS, nqp 18310f0a2 (BASE 5f46163d2), 40/40 runtime tests. Concerns and rulings:
  - Ruling: the commit trailer must read `Co-Authored-By: Claude Fable 5.1` (user rule: the trailer names the directing session); the implementer used its own model name -> amend in the fix round. Cost if wrong: none (message only).
  - Ruling: `Unpersistable` keeps its stack trace — thrown ~50 times per training run (C0), not a hot path. Cost if wrong: milliseconds at training exit.
  - Ruling: ArgKind/ResumeKind persist by declaration index — accepted; the slots are regenerated by every build, never read across enum changes without a rebuild. Cost if wrong: a stale artifact mis-realises -> the verify gate.
  - Ruling: a malformed slot (a corrupt PDescriptor) is a hard error, like a slot that does not decode (restore throws IllegalStateException); only writer-produced artifacts exist. Cost if wrong: a hand-corrupted artifact dies at first miss instead of recording.
Task 3: review dispatched (package task-3-review-package.md).
Task 2: complete (controller). Rebased rakudo b114238897 (214 over origin/main), gate make 869 s wall (CORE.c parse stage 220 s), t/01-sanity 25/25 in 62 s, pushed --force-with-lease to ab5tract. nqp upstream/main: 0 new commits, no rebase.
Task 3: review = Approved, spec ❌ on the trailer only; one Important finding (plan-mandated): realise() resolves an HLL guard through tc.gc.getHLLConfigFor(name), which reads the CURRENT of two maps (compilee/compiler) and creates on a miss; Guard.OfHll compares by identity, so a config from the wrong map makes the restored program permanently dead (never a wrong answer).
  Ruling (overrides the brief's Interfaces line): persist the HLL guard as (name, compiler-side flag); HLLConfig gains `@JvmField var compilerSide: Boolean` set by GlobalContext.getHLLConfigFor from which map is current; GlobalContext gains a non-creating `findHLLConfig(name, compilerSide): HLLConfig?`; realise throws Unpersistable when it is null. Why: the type's hllOwner is assigned from whichever map was current at deserialize (SerializationReader.kt:578), so the name alone does not identify the object. Cost if wrong: an HLL-guarded program is dropped or dead at restore -> the site records as before; the verify gate sees it as unseen, the counters as dropped=.
  Minor (deferred): a kitchen-sink round-trip test over every P-type (PResumeInitArg, PAttribute, PHow, PUnbox, PLookup, PResumeState, PGuardNotLiteralObj, POutcomeValue/Syscall, PResumption, PLevel, PBind, bindFailure, NUM literal, named descriptor, resumeKind != NONE); PLiteral.i comment on Long normalisation; `catch (e: Exception)` unused binding at DispatchSlotCodec.kt:183; `!!` after ref() in DispatchDump.kt:60,69; non-Unpersistable throwables (ClassCastException, CallSiteDescriptor ctor) escape persist/realise (ruled hard errors, see above).
Task 3: fix round 1/5 dispatched (resume implementer): HLL finding + trailer amendment.
Task 3: fix round 1/5 (2 addressed per implementer, 0 open pending re-review — HLL guard as (name, compilerSide) via GlobalContext.findHLLConfig; trailer amended; commit amended 18310f0a2 -> e27a795d8, 41/41 tests; deviations reported: GlobalContext init hoists compilerHLLConfiguration two lines up; test guards on getHLLConfigFor("nqp") since KnowHOW's hllOwner is null). Re-review dispatched (task-3-fix1-package.md).
Task 3: fix round 1/5 re-review: 2 addressed, 0 open, no new breakage. Deferred minor (new): DispatchSlotCodec.kt:96 persists `g.hll?.name` (String?) — a non-null config with a null name would persist as a null-HLL guard; unreachable today (getHLLConfigFor assigns name before put).
Task 3: complete (nqp commits 5f46163d2..e27a795d8, review clean after 1 fix round)
Task 4: implementer dispatched (BASE nqp e27a795d8; brief task-4-brief.md)
Task 4: implementer DONE, nqp 953251714 (BASE e27a795d8), 42/42 tests. Smoke (rebased build, slots empty): dispatch stats hits=80298 misses=5056 sites=6522 anon=5 sitesAll=6596 restored=0 restoredSites=0 dropped=0 recorded=4723; runtime-made sites = 74; verify: on / matched=0 mismatched=0 unseen=4606. Note: recorded (4723) < misses (5056) as expected (tail hits count as misses); recorded is the Task 5/6 baseline. Review dispatched (task-4-review-package.md).
Task 4: review = Approved, spec ✅; Important: verify()'s match/mismatch branches never executed (no test, smoke only hits unseen) -> fix round 1 (a test over matched/mismatched/unseen). Deferred minors: unrecognised NQP_DISPATCH_PERSIST values mean ON (brief-verbatim); racing first misses can install duplicates (plain `restored` field; harmless, wastes cap slots, double-counts); verify describes every recording even when kept is empty (verify-only cost); restored/restoredSites also count in VERIFY mode; counters process-wide, not cleared by resetAll (same as the pre-existing ones — Task 5/6 ledger must say which population a figure covers); verify's `recorded` parameter shadows the counter; the test registers a namespace in the process-wide registry (use unique names per test); store()/entryPrefix unused until Task 5.
Task 4: fix round 1/5 dispatched (resume implementer).
Task 4: fix round 1/5 (1 addressed per implementer: verify test over matched/mismatched/unseen; test-only; commit amended 953251714 -> 5fd74d9b6; 43/43 tests). Re-review dispatched (task-4-fix1-package.md).
Task 4: fix round 1/5 re-review: 1 addressed, 0 open, no new breakage. Deferred minors (new): verify test case (a) does not assert silence; the test's same-descriptor instance short-circuits Captures.sameShape.
Task 4: complete (nqp commits e27a795d8..5fd74d9b6, review clean after 1 fix round)
Task 5: implementer dispatched (BASE nqp 5fd74d9b6; brief task-5-brief.md)
Task 5: implementer DONE, nqp 3d0b54fa4 (BASE 5fd74d9b6), 45/45 tests, jars synced (no training run). Concerns noted: selected() dereferences recordSelector!! (only the hook calls recordAtExit); rewrite uses a fixed <path>.tmp (two concurrent training runs on one artifact would race — none exist in the build). Review dispatched (task-5-review-package.md).
Task 5: review = Approved, spec ✅, no Critical/Important. Deferred minors: patch() does not check MAGIC/VERSION; an empty named slot writes off/0 silently; fixed <path>.tmp (race on concurrent training; tmp left on a failed move); selected() NPEs if recordAtExit() is called without the env var; programs dropped by the MAX_PROGRAMS cap are not counted on the marker line; fully-qualified UnitDispatchWriter call; the test's surviving slot stays at offset 0 (a moved unnamed slot is untested); no test for an absent nested prefix / empty newSlots.
Task 5: complete (nqp commits 5fd74d9b6..3d0b54fa4, review clean)
Task 6: dispatched (operational subagent; brief task-6-brief.md; report-only, no code edits)
Task 6: BLOCKED on verify (report task-6-report.md, logs ~/.claude/jobs/ba3ab3a7/tmp/task6/). Numbers: rakudo consumer after training (one run, stats on) 1.943 s: hits 13292 misses 4306 sites 6438 sitesAll 6511 restored 4477 restoredSites 4195 dropped 37 recorded 193 (from 4723); nqp: recorded 1766 -> 87. Verify: rakudo cold matched 4475 mismatched 4 unseen 127; nqp cold matched 1679 mismatched 8 unseen 41; warm t/01-sanity matched 113074 mismatched 313 unseen 71166. t/01-sanity default 25/25 in 40 s; under verify 24/25 in 45 s (55-use-trace.t compares a child's stderr, broken by the `dispatch-verify: on` print). Sizes: CORE.c 56077905 -> 56362465 (unit.dispatch 284060), QAST.jar 750084 -> 1239373 (489289), v6c 11348915 -> 11735401 (386486). Erosion: training rakudo after nqp loses ~1.3 % of nqp's slot programs (the 37 dropped at restore are not re-persisted).
  Ruling 17 (verify semantics): a persisted program whose guards pass is compared first by text (matched), then by EVALUATED OUTCOME on the recorded call's arguments -- same outcome kind, the same callee object by identity / the same syscall name / the same value, the same evaluated argument capture, the same resumption dispatchers and init captures, equal bindControl; resuming programs stay text-only -- counted `byOutcome`; only a program that fails both is a MISMATCH. Why: nqp's lang-meth-call records a type-guarded form before a class publishes its method cache and a cache-lookup form after; verify mode, by not installing, makes the site re-record after the cache exists, so the FORM differs while the target is the same. The gate is mismatched=0; byOutcome is reported. Cost if wrong: a program whose outcome coincides on the training call but whose guards are too weak passes verify -- the same invariant replay already rests on.
  Ruling 18 (verify output): all verify lines go to the file named by NQP_DISPATCH_VERIFY_LOG (append, one process's lines prefixed by its pid, flushed per line) when set, else to stderr as today. Why: a test that compares a child's stderr (55-use-trace.t) breaks on the `on` marker, and TAP swallows per-run MISMATCH blocks. Cost if wrong: none (diagnostics routing).
  Ruling 19 (drop diagnostics): NQP_DISPATCH_PERSIST_TRACE=1 prints `dispatch-persist: dropped <identity> <reason>` per dropped program (DispatchSlotCodec.realise gains an optional onDrop callback carrying the Unpersistable message). Why: the 37 drops / the 1.3 % erosion need a cause before Task 7 bakes training into the build. Cost if wrong: none.
  Ruling 20 (erosion): a slot rewrite carries restored ∪ new minus what did not resolve in the training process; accepted for now, revisited with the drop reasons. Cost if wrong: a few nqp-side sites record once more under Rakudo.
Task 4: fix round 2/5 dispatched (resume implementer) for rulings 17-19.
Task 4: fix round 2/5 (rulings 17-19 applied; NEW commit nqp d3e602917 on top of Task 5's 3d0b54fa4 — amending would have rewritten Task 5; squash at the next stack rewrite; 46/46 tests; jars synced; cold rakudo verify on the trained artifacts: matched=4468 byOutcome=4 mismatched=0 unseen=720; a first pass had silently dropped Task 5's recorder from DispatchPersist.kt, caught in self-review and restored). Re-review dispatched (task-4-fix2-package.md).
Task 4: fix round 2/5 re-review: 4 addressed (rulings 17-19 + recorder intact), 0 open, no new breakage. Deferred minors (new): verifySay prefixes only the first physical line of a MISMATCH block; the drop trace prints to stderr, not the verify log; sameOutcome ignores resumeLevels/bindFailureProgram (vacuous for non-resuming); no test pins that the NQP_DISPATCH_RECORD hook is installed (a whole-file rewrite dropping recordAtExit compiled clean).
Task 4: complete again (nqp d3e602917 added after Task 5's 3d0b54fa4; squash into 5fd74d9b6 at the next stack rewrite)
Task 6: re-run DONE (report task-6-report.md "Re-run"; logs ~/.claude/jobs/ba3ab3a7/tmp/task6/rerun/): nqp cold verify matched 1658 byOutcome 8 mismatched 0 unseen 62 (1.297 s); rakudo cold matched 4475 byOutcome 4 mismatched 0 unseen 127 (2.627 s, verify on); warm t/01-sanity under verify 25/25 in 43 s, log totals summed matched 117490 byOutcome 327 mismatched 0 unseen 73893; default-mode sanity 25/25 in 42 s. Drops: all `no SC <handle>` -- 5 handles absent from every jar (runtime/compile-time SCs that do not exist yet at the site's first miss, or are per-compile), 37 rakudo / 30 nqp, ~0.8 % of installed programs; they re-record as before. Ruling 20 confirmed.
Task 6: complete (no commits; numbers in both ledgers)
Task 7: implementer dispatched (BASE nqp d3e602917 / rakudo b114238897; brief task-7-brief.md)
Task 7: implementer DONE_WITH_CONCERNS: nqp f5c5bc8fa (gradle: stage2Trained Sync + trainDispatch JavaExec + syncLib from the copy), rakudo 72417e05e1 (Makefile.in stamp target + runner prerequisite). Gate (default mode): clean buildJvm 218 s (9 jars trained); nqp suite 155/155 green, 196 s; make clean+make 883 s (+++ Training once; CORE.c parse 221 s, stage sum 282 s); t/01-sanity 25/25, 55 s; cold stats restored=4475 recorded=195. Deviation: `touch -r` lines normalise the mtimes of the rewritten artifacts + settings + bootstraps before the stamp so a second make is a no-op (the brief's recipe made a second make recompile the frontend and settings, ~900 s). Adaptations: commons-io absent -> OutputStream tee fallback; `java.io.*` import needed (the DSL's `java` extension shadows the package). Concern: neither training gate asserts a quantity. Review dispatched (task-7-review-package.md, both trees).
Task 8: dispatched in parallel with Task 7's review (operational; runs the built tree only; brief task-8-brief.md).
Task 7: review = Approved; three Important findings.
  Ruling 21 (determinism): training is reproduced by any clean build up to the run's own nondeterminism (NQPCORE 103/114/9 vs 102/112/8 slots/programs/unpersistable across three clean buildJvm runs, ~1 % of slots); the plan's "deterministically" is relaxed to that; the verify gate is the correctness net; the cause (execution-order variation in the nqp training run) is recorded in the findings, a two-dump diff of two gradle trainings deferred. Cost if wrong: a cache keyed on jar bytes (gradle build cache) misses; no behavioural cost.
  Ruling 22 (gate order): the nqp suite in default mode runs on the POST-make lib jars as part of Task 8 (Task 7's suite ran on the gradle-trained jars before make retrained them). Cost if wrong: an untested lib-jar state.
  Fix round 1/5 (Task 7): the Makefile training line's `tee` masks the runner's exit status -> redirect to the log and test the exit (`|| { cat log; exit 1; }`), then cat the log. Deferred minors: the touch -r window for a source edited during the build (documented); no quantity floor on either marker; greedy `sed ... to ||p`; configuration-cache-hostile tee objects; engineJarFile an input by classpath snapshot only; blib/.dispatch-trained + .dispatch-train.log not in the make cleanups nor .gitignore; absolute paths in the marker.
Task 7: fix round 1/5 applied (rakudo 72417e05e1 -> 79829d402e amended: the training line writes its log and tests the exit, POSIX sh; failure path proved by hand: old tee shape exit 0/0, new shape exit 1; happy path exit 0). Validation (Configure --gen-nqp, rm stamp + make, second make no-op) HELD until Task 8's gates finish (they run on the lib jars --gen-nqp would re-sync and make would retrain). Re-review after the validation.
Task 8: DONE (report task-8-report.md; logs ~/.claude/jobs/ba3ab3a7/tmp/task8/). Verify gate: nqp suite 155/155 in 204 s, 11 processes, matched 271378 byOutcome 1309 mismatched 0 unseen 116617; t/01-sanity 25/25 in 51 s, 2 processes, matched 117441 byOutcome 327 mismatched 0 unseen 73941; zero MISMATCH blocks. Off gate: nqp suite 155/155 in 201 s, sanity 25/25 in 49 s, results identical to the default reference (196 s / 56 s). Ruling 22 run: default-mode nqp suite on the post-make lib jars 155/155 in 206 s. No tooling change (the sweep passes the environment through). Note: the sweep silently accepts a non-existent explicit target (deferred hardening).
Task 8: complete (no commits)
Task 7: validation released.
Task 7: fix round 1/5 validated: Configure --gen-nqp 5 s (gradle retrained the stage2 copy, BUILD in 2 s); rm stamp + make = a FULL build, 880 s (Configure re-expands gen/jvm/main-version.nqp, invalidating everything -- pre-existing), Training once, 21 dispatch-record lines (CORE.c 1486 slots, v6c 841, QAST 1111, rakudo.jar 35), CORE.c parse 224 s / stages 284 s; second make no-op; cold stats restored=4475 recorded=195. Re-review dispatched (task-7-fix1-package.md).
Task 9: dispatched in parallel with Task 7's re-review (rig row c on the final build, docs, memory; rebase + push at the end; brief task-9-brief.md).
Task 7: fix round 1/5 re-review: 1 addressed, 0 open, no new breakage. Deferred minors (new): training output no longer streams during the run (a silent gap under watched-run, harmless at -e '' length); the `sed | xargs touch -r` line is still a pipeline (cosmetic); any Configure.pl invalidates the whole build (pre-existing).
Task 7: complete (nqp d3e602917..f5c5bc8fa, rakudo b114238897..79829d402e, review clean after 1 fix round)

## Rig rows

| tag | rakudo hash | nqp hash | cold rakudo-e (s) | cold nqp-e (s) | misses | hits | warm (s) | new red | verdict |
|---|---|---|---|---|---|---|---|---|---|
| b | 107eca63a3 | 318558c2d | 2.461 | 1.160 | 5667 | 100711 | 50/sanity | none | Phase B close, the baseline |
| c | 79829d402e | f5c5bc8fa | 2.272 | 1.203 | 4931 | 35512 | 63/sanity | none | Phase C close, trained build; restored=4470 recorded=849 sitesAll=7569 dropped=37 (best cold rakudo-e); nqp restored=1644 recorded=257 |
| close | 6217a89e61 | a837bf1bb | 2.247 | 1.198 | 4931 | 35512 | 59/sanity | none | milestone close, fix-wave build; restored=4470 recorded=849 sitesAll=7569 dropped=37 staleSchema=0 (best cold rakudo-e); nqp restored=1644 recorded=257 staleSchema=0 |

Task 9 (2026-09-16): rig row `c` on the phase's final build (5 cold runs each, `--warm=proxy`, no NQP_DISPATCH_RECORD in the environment; rig walls rakudo 2.27 2.34 2.38 2.31 2.29, nqp 1.29 1.23 1.20 1.22 1.24). Stats line, best cold rakudo-e (`-e 'say 1'`): `hits=35512 misses=4931 sites=7463 anon=6 sitesAll=7569 restored=4470 restoredSites=4188 dropped=37 recorded=849`; best cold nqp-e: `hits=9998 misses=1829 sites=3012 anon=4 sitesAll=3059 restored=1644 restoredSites=1634 dropped=30 recorded=257`. Histogram left on rakudo: lang-meth-call 2747, lang-call 1950, boot-syscall 197, raku-assign 35, raku-coercion 1, raku-meth-call-qualified 1. Observation to carry: the warm proxy clock reads 63 s against row b's 50 s, while the same directory under `t/harness5 --evalserver` read 49-55 s across Tasks 6-8 against Phase B's 58-60 s -- single samples on both sides, not re-run (user rule), flagged in the findings as an open item.
Task 9: complete (rakudo 165c477f2b docs + a896b743e0 hash mapping after the rebase; both trees rebased onto one new upstream commit each and pushed: rakudo a896b743e0, nqp e3c800371; rig row c: 2.272 / 1.203 / 4931 / 35512 / 63 s sanity; open item: warm proxy 63 s vs 50 s, not re-taken; the rig's `say 1` records 849 vs the build's `-e ''` training).
Final whole-branch review dispatched (final-review-package.md: nqp a5506673e..e3c800371 + rakudo code diffs 6af2cfa67f..a896b743e0). Task 10 dispatched in parallel (build, rig row close, whole t/, summary docs, push).
Final whole-branch review: "ready with fixes"; 0 Critical, 5 Important, 12 Minor. Important: (1) no schema version in the slot bytes + TRAIN_STAMP does not depend on the runtime jars (a runtime-only rebuild + make does not retrain; an untagged codec mis-decodes silently); (2) recordAtExit has no error containment: a throwable in the hook leaves a green half-trained build (markers satisfied by earlier paths); (3) NQP_DISPATCH_RECORD=all exported by hand rewrites any loaded artifact incl. the uncommitted stage0 jars; (4) UnitDispatchWriterTest never exercises a MOVED unnamed slot (the one branch that could corrupt every later read); (5) stage2Trained/trainDispatch overlapping gradle outputs (always-retrain is emergent, not designed). Triage: two deferred minors reversed (moved-slot test; throwables escaping persist -> containment in the recorder).
  Ruling 23 (reverses the T5 deferral): the moved-unnamed-slot test is mandatory. Ruling 24 (extends ruling on hard errors): a malformed slot stays a hard error in restore; the RECORDER contains every throwable per program and per path, prints `dispatch-record: FAILED <path> <reason>`, ends with `dispatch-record: done <paths> paths, <slots> slots`; both build markers require the done line and the absence of FAILED (the quantity floor). Ruling 25: DispatchSlot gets a leading `SCHEMA` int (1); restore treats a mismatch as an empty slot; TRAIN_STAMP also depends on $(RUNTIME_JAR) and $(NQP_RUNTIME_JAR). Ruling 26: the recorder refuses any path under src/vm/jvm/stage0 and prints the path list it will rewrite; `all` stays the builds' selector. Ruling 27: the gradle marker moves out of the synced dir, the trained jars are declared outputs, and the task is documented as intentionally always out of date. Also in the wave: the kitchen-sink round-trip test, a test that the record hook is installed, blib/.dispatch-trained + .dispatch-train.log in CLEANUPS and .gitignore, the two one-character cleanups (catch binding, `!!` after ref).
Task 10 STOPPED for the fix wave (it will be restarted on the fixed tree; the close's numbers come from that build).
Task 10: stopped cleanly (Configure 4.85 s ran; make killed at ~1150 s inside CORE.c; nothing committed; blib incomplete until the close's make; nqp lib jars hold current-codec slots -> the fix wave's clean buildJvm retrains them). Facts for the close: whole-t/ population is now 482 files (13 non-spec dirs, t/11-js excluded) vs the 5078 s baseline's 427; docs/jvm-full-suite-run-2026-09-05.md exists in no commit on this branch; MemAvailable 25.7-26.6 GB during the build (the band of Phase B's guard kills).
Fix wave dispatched (F1 schema version + stamp deps; F2 recorder containment + done/FAILED markers; F3 stage0 refusal; F4 moved-slot test; F5 gradle outputs; kitchen-sink and recorder end-to-end tests; .gitignore/CLEANUPS; two one-char cleanups).
Fix wave: DONE — nqp a837bf1bb (BASE e3c800371), rakudo 6217a89e61 (BASE a896b743e0); 50/50 tests; clean buildJvm 223 s: done 9 paths, 1683 slots, 1711 programs, 41 unpersistable, 0 failed; repeat buildJvm 1 s (stage2Trained + trainDispatch only, byte-identical retrain); nqp stats restored=1677 staleSchema=0 recorded=87.
  Ruling 28: stage2Trained is also `upToDateWhen { false }` (the always-retrain had been emergent from the marker inside the synced dir); each build trains fresh untrained jars. Cost if wrong: ~2 s per build.
  Deferred (new): `done`'s unpersistable and failed counts overlap for a program whose persist threw; no slot-quantity floor on the markers (still open); any build.gradle.kts edit recompiles both stages (pre-existing).
Fix wave re-review dispatched (fixwave-package.md, both trees).
Task 10: resumed on the fixed tree (rakudo 6217a89e61 / nqp a837bf1bb): build + sanity, then the verify gate again (schema changed), then rig row close; HOLD before whole t/ until the fix-wave re-review is clean.
Fix wave re-review: all findings ADDRESSED, no new Critical/Important breakage. Minor (new): kitchen-sink test leaves UINT / null-object literal / null classHandle uncovered (siblings covered); the recorder test leaves a site and a store in the process-wide registries; TRAIN_STAMP does not depend on the nqp-truffle jar (cannot change slot content).
Final review: CLOSED (one fix wave, one scoped re-review, residuals parked as minors above). Task 10 given the go-ahead for whole t/ + docs + push.
